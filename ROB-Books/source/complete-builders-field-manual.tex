\documentclass[10pt,openany]{book}
\usepackage{robbook}
\usepackage[
  contentBlocks,
  fencedCode,
  pipeTables,
  relativeReferences,
  smartEllipses,
  tableCaptions,
  taskLists,
  texMathDollars,
  tightLists=false
]{markdown}
\SetROBAccent{ROBRed}
\SetROBVolume{COMPLETE FIELD MANUAL - ADVANCED}{BUILDING R.O.B.}
\SetROBChapterPrefix{Chapter}
\lstdefinelanguage{text}{}
\lstdefinelanguage{swift}{}
\lstdefinelanguage{objc}{}
\lstdefinelanguage{json}{}

% The combined manual reaches three-digit chapter numbers and section numbers
% such as 16.15.7.  The standard book-class TOC boxes are too narrow for them.
\makeatletter
\renewcommand\@pnumwidth{2em}
\renewcommand*\l@chapter[2]{%
  \ifnum \c@tocdepth >\m@ne
    \addpenalty{-\@highpenalty}%
    \vskip 1.0em \@plus\p@
    \setlength\@tempdima{3.2em}%
    \begingroup
      \parindent \z@ \rightskip \@pnumwidth
      \parfillskip -\@pnumwidth
      \leavevmode \bfseries
      \advance\leftskip\@tempdima
      \hskip -\leftskip
      #1\nobreak\hfil
      \nobreak\hb@xt@\@pnumwidth{\hss #2\kern-\p@\kern\p@}\par
      \penalty\@highpenalty
    \endgroup
  \fi}
\renewcommand*\l@section{\@dottedtocline{1}{1.5em}{3.4em}}
\renewcommand*\l@subsection{\@dottedtocline{2}{4.9em}{4.6em}}
\renewcommand*\l@subsubsection{\@dottedtocline{3}{9.5em}{5.6em}}
\makeatother

\begin{document}
\ROBTitle{Building R.O.B. - Complete Field Manual}{BUILDING R.O.B.}{The complete maker's field manual: fabrication, circuits, firmware, perception, identity, control, and operations}{Advanced evidence draft with builder placeholders}{2025-rob-side-full-body.jpg}

\ROBFrontMatter{An honest build record}{Complete in scope, intentionally incomplete where the archive cannot prove the detail.}{%
This manual connects ROB's 95-page design presentation, private construction-photo archive, Arduino base firmware, motor references, Cerebro v1-v5 software archive, and current Mac, iPhone, Watch, Vision Pro, lidar/map, three-camera, serial, MLX/VLM, Gemini, face-identity, Messages, recording, stage-show, arm, autonomy, H.264, and secure-control source trees.

It is a first evidence-based edition, not a stamped mechanical design, production wiring schematic, safety certification, or permission to energize the machine. Use the status labels on every subsystem. Replace each Builder input needed box with measured, reviewed information as the book iterates.

\begin{center}
\includegraphics[height=0.27\textheight]{rob-used-future-frontispiece.png}\\[-1pt]
{\scriptsize\itshape Original illustrated portrait inspired by ROB; it is visual storytelling, not engineering evidence.}
\end{center}
}

\section*{Evidence-status legend}

\begin{tabularx}{\textwidth}{p{1.75in} X}
\Implemented & Confirmed in the inspected current source or directly visible hardware evidence.\\[5pt]
\Commanded & Software sends a value, but this review did not measure the physical outcome.\\[5pt]
\Experimental & Present behind a gate, dependent on external hardware, or still under active development.\\[5pt]
\Legacy & Retained compatibility or historical behavior, not the preferred new design.\\[5pt]
\Planned & Described in a slide, TODO, protocol, or interface without proof of completed execution.\\[5pt]
\Unverified & The builder must measure, reconcile, or supply the missing record.\\
\end{tabularx}

\begin{safetybox}[title={NON-NEGOTIABLE SAFETY BOUNDARY}]
ROB combines large batteries, high-current brushless motors, chains, treads, flippers, linear motion, long arms, machine-tool fabrication, networking, cameras, and software-controlled movement. Qualified adults must own the hazard analysis, power protection, emergency-stop architecture, workholding, guarding, lifting, test perimeter, and public operation. Never infer a safe circuit or fabrication dimension from a photograph. Never treat an ordinary IR sensor, software timeout, or cloud model as safety-rated protective equipment.
\end{safetybox}

\ROBArduinoStatus
\ROBAmberStatus
\ROBSourceRoot

\section*{Source snapshot and publication cautions}

The initial evidence pass began on 2 August 2026; this expanded hardware, AMBER, model-training, identity/privacy, mapping, and three-feed-video edition reflects the repositories inspected through 23 August 2026 and the builder interview of 21 August. ROBControllerVision's latest three panorama-orientation commits were still ahead of its public remote. Cross-repository publication still requires an intentional tagged/tested release. Original gallery photographs were converted with orientation applied and metadata stripped. Attendee photographs, especially images containing children, require release review or careful exclusion.

The archived B-COMMAND drawings prohibit reproduction without permission, and the HengDrive drawings are marked proprietary/confidential. This manual does not reproduce the drawings or internal artwork. It records selected interface facts and ratings needed to identify and operate the builder-owned parts, distinguishes installed hardware from recollection, and flags every conflict. Obtain written publication permission or public replacement datasheets before distributing those sections beyond the builder's private record.

\tableofcontents
\clearpage
\mainmatter

\part{Intent and architecture}

\ROBChapterPhoto{Chapter 1}{The robot as a long-running experiment}{2025-rob-face-portrait.jpg}

\chapter{Design history and current scope}

ROB is not one frozen machine. The archive shows a succession of working prototypes, public demonstrations, repair decisions, new sensors, drivetrain modifications, camera changes, arm work, and software migrations. A useful build manual must preserve that timeline because the answer to ``How is it built?'' depends on the date and subsystem.

\section{The one hard constraint}

The project began with the purchase of the SuperDroid Robots LT2 tracked platform. That tread system was the one interface the builder chose not to redesign. Everything else---body width, battery placement, drivetrain packaging, flipper geometry, service openings, cable paths, and eventually the upper robot---had to negotiate with it.

The first design instrument was cardboard. The builder mocked the LT2 envelope at full scale, moved imagined components until they fit, and used successful faces as templates for maple plywood. A third-wheel/flipper idea drew inspiration from Johnny 5 in \textit{Short Circuit}: a perpendicular wheel might lift part of the contact patch and reduce the sideways scrub required for a heavy tracked robot to turn. This is not merely a charming origin story. It is the repeatable method behind ROB:

\begin{enumerate}
  \item freeze the purchased interface and record its revision;
  \item make a cheap full-size representation;
  \item write one question on each prototype;
  \item test packaging, reach, sweep, balance, access, and cable bend before strength;
  \item transfer only proven geometry into plywood or metal;
  \item preserve failed versions and the decision they changed.
\end{enumerate}

\begin{advancedbox}
A cardboard prototype proves geometry only within its scale and stiffness. It cannot certify structural strength, fastener retention, tread tension, electrical insulation, thermal behavior, payload, stopping distance, or public safety. Record both what each mockup proved and what it could not prove.
\end{advancedbox}

\section{Evidence-based chronology}

\begin{tabularx}{\textwidth}{>{\robcondensed\bfseries\color{ROBAccent}}p{0.72in} X X}
\toprule
PERIOD & VISIBLE OR DOCUMENTED STATE & EDITORIAL INTERPRETATION\\
\midrule
2019 & Functional black tracked robot with screen torso, earlier camera head, exposed arms, and movement demonstrations. & An integrated early prototype existed; exact internal revision and specifications are not documented.\\
2020-2021 & Open electronics, camera/head revisions, wooden base templates, tread geometry, battery layout, and plans for CNC aluminum. & The builder was using accessible materials to resolve packaging and mechanics before a final enclosure.\\
2022 & Mini computer and serial cabling, base brackets, drilling, actuator hardware, arm electronics, OAK-D camera, and public appearances. & Major integration work joined mechanics with onboard computing and sensing.\\
2023 & Public use and head/camera refinements, with fewer construction photographs. & Operational experience likely drove requirements, but the archive does not link each change to a written test.\\
2024 & Sprocket-area machining, chain clearance, tread measurement, internal rewiring, batteries, actuator, voltage gauge, and controller board. & A substantial drivetrain and base-serviceability revision is strongly documented.\\
2025 & Shoulder and neck gearing, arm removal/reassembly, cable labels, emergency-stop hardware, Mac software, vision, mapping, and chess reach studies. & The current visual identity and advanced software experiments are visible, while final calibration remains in progress.\\
\bottomrule
\end{tabularx}

\ROBPhotoTwo{2019-rob-front-corner.jpg}{The 2019 machine already joins a tracked base, arms, sensors, and operator-facing character.}{2025-rob-side-full-body.jpg}{A 2025 side view shows the newer spherical camera head, speaker face, neck mechanism, articulated arm, and tracked base.}

\section{Cerebro's recovered software roots}

The current public Cerebro repository begins on 5 August 2025 with a commit that calls itself an initial v5 change in a fresh repository. The preserved v1-v5 workshop folders show that this is a migration boundary rather than Cerebro's beginning.

The pre-Git v1 folder retains an upstream Cocoa Arduino serial example dated 2009. That date belongs to the starting example, not ROB. ROB-specific headers and project state date the adaptation to 2017-2018: the Mac application is named Cerebro, opens a 250000-baud USB serial path, and issues forward, reverse, turn, flipper, and linear-actuator commands in ROB's seven-field text frame. Keyboard and multipeer scaffolding show the command station beginning to accept additional operator inputs.

The first surviving Cerebro Git history starts with \texttt{0141a646} on 1 January 2018. During 2018, v2 adds speech, separate Base/Head/Torso serial paths, bound Cocoa windows, SceneKit and multipeer controllers, OpenNI/PCL/NiTE point-cloud and user-tracking experiments, a named consciousness bridge, and Leap Motion. The later v3 archive records controller arbitration and autonomy permission, Maestro servos, multilingual speech, ReSpeaker and RealSense task processes, human/head tracking, RTSP, T265, performance work, AutoNet, and the 2022 RPLidar repository split. A related v4 branch carries the old 2018 root through Apple-silicon, USB/network, Google-LLM, singleton-process, continuous-speech, and Foundation Models work in July 2025.

The v5 root \texttt{4c4f1d4} has no parent, but its imported tree already contains the accumulated robot program. Preserve the old Git stores and file fingerprints separately: v3 and v4 are related branches rather than one invented straight line, and third-party examples and SDKs retain their own attribution and licensing. The detailed archive record is \texttt{ROB-Books/CEREBRO\_ARCHIVE\_HISTORY.md}.

\section{Design principles that recur}

Across the sources, several durable principles emerge:

\begin{itemize}
  \item use prototypes to discover packaging and load problems;
  \item separate high-level intent from low-level actuator values;
  \item keep independent stop layers close to each source of failure;
  \item isolate optional cameras, Python helpers, video, speech, and AI from core motion control;
  \item preserve service access and label wiring;
  \item make unsupported capability fail honestly;
  \item collect evidence before calling a proposal implemented.
\end{itemize}

\section{Current subsystem status at a glance}

\begin{tabularx}{\textwidth}{p{1.42in} p{1.4in} X}
\toprule
SUBSYSTEM & STATUS & EVIDENCE-BASED SUMMARY\\
\midrule
Tracked base & \Implemented & Physical treads, chains, drive motors, brackets, batteries, and actuator are visible across multiple revisions. Exact final dimensions and performance are missing.\\
Arduino base & \Implemented & Historical firmware parses a seven-field frame and commands treads, flipper, and actuator; it reads IMU and IR data. It has significant safety and parser defects.\\
Flipper motion & \Commanded & Firmware and controller paths exist. Final geometry, limits, load rating, and measured motion are unverified.\\
Torso / third-wheel actuator & \Commanded & Serial control and physical actuator evidence exist, but the presentation assigns conflicting roles across revisions.\\
Mac mini / Cerebro & \Implemented & Central macOS coordination, serial, camera, speech, secure control, and autonomy infrastructure exist in source.\\
Phone controller & \Implemented & Established control UI, pairing, autonomy, and action-console paths exist.\\
Watch controller & \Implemented & Freshness-checked motion and speech intent relay through the phone.\\
Vision Pro controller & \Experimental & Secure control ownership, simulator, independent arm leases, three H.264 feeds, and mixed-immersive panorama exist; hardware/release validation remains necessary.\\
RGB-D / 360 cameras & \Experimental & Separate face/belly DepthAI helpers and headless Insta360 control/perception exist; device, calibration, capacity, and environment validation remain necessary.\\
RPLidar & \Experimental & Compact scan/map publication, authenticated local IPC, map persistence, and base-map alignment exist; proprietary framework/hardware validation remains unresolved.\\
Arms / neck & \Commanded & Servo, AMBER gateway, keyframe, feedback, and Vision joint-jog paths exist; calibration, final limits, collision safety, and HIL completion remain incomplete.\\
Face identity & \Experimental & Disabled-by-default consent enrollment, encrypted gallery, open-set/temporal recognition, and two AdaFace encoders exist; liveness and on-ROB biometric calibration remain open.\\
Messages bridge & \Experimental & Disabled-by-default allowlisted one-to-one AI, bounded images, optional encrypted memory, and exact-confirmation local scripts exist; permissions, retention, and production privacy review remain open.\\
Training recording & \Experimental & Explicit recoverable synchronized sensor/video sessions exist; consent, storage, dataset governance, and model promotion remain operator responsibilities.\\
Gemini actions & \Experimental & The multimodal session defaults off, while audio/video default on once that session is enabled unless separately disabled. The action tool is separately default-off; only \texttt{stop\_motion} has a partial local executor.\\
\bottomrule
\end{tabularx}

\ROBPlaceholder{Builder narrative: add the project's origin, meaning of R.O.B., design goals, major failures, people and shops who contributed, dates of each hardware revision, and which public demonstrations changed the design.}

\ROBChapterPhoto{Chapter 2}{System boundaries and ownership}{2024-open-tracked-base.jpg}

\chapter{Whole-robot architecture}

\ROBControlStackDiagram

\section{Physical and information paths}

The robot contains two overlapping architectures:

\begin{enumerate}
  \item The \textbf{energy architecture} moves stored electrical energy through protection, conversion, controllers, conductors, and motors.
  \item The \textbf{information architecture} moves intent and measurements through interfaces, authentication, state machines, protocols, adapters, firmware, and feedback.
\end{enumerate}

The two paths meet at motor controllers and actuators. Keeping their documentation separate prevents a common error: assuming that a correct data value proves the high-current circuit is safe, or that a correctly wired motor proves the command path is authorized.

\section{Desired component ownership map}

The table below states the target responsibility split. It is a design contract, not a claim that every current implementation already satisfies every responsibility; gaps must remain visible in the subsystem status and verification chapters.

\begin{tabularx}{\textwidth}{>{\robcondensed\bfseries}p{1.32in} X X}
\toprule
OWNER & PRIMARY RESPONSIBILITY & MUST NOT ASSUME\\
\midrule
Human operator & Select mode, maintain line of sight and perimeter, respond to hazard, own start and stop. & That autonomy, sensors, or remote viewers replace supervision.\\
Controller app & Capture fresh intent, apply its client-specific lease or watchdog, present authoritative mode and stop state. & That sending a packet proves movement occurred.\\
Secure transport & Authenticate device, authorize role, bound and order messages, reject replay and stale sessions. & That an authenticated controller's payload is physically safe.\\
Cerebro & Arbitrate control master, coordinate subsystems, validate high-level actions, adapt to hardware protocols. & That optional camera, AI, or lidar availability can weaken manual stop.\\
Arduino base & Target: accept only complete bounded base commands, own a local deadline, and drive low-level pins and actuator serial. & That the current historical parser already meets this target, or that any arriving byte is a valid command or heartbeat.\\
Motor electronics & Enforce rated electrical behavior, current limits, and safe-start rules. & That software alone prevents wiring or load faults.\\
Mechanical system & Carry loads, constrain motion, guard hazards, survive declared environments. & That commanded direction, speed, or stop equals measured behavior.\\
\bottomrule
\end{tabularx}

\section{Sense-plan-act without surrendering authority}

\ROBSimpleSystemDiagram

Manual control, deterministic autonomy, and AI-assisted behavior can all use the same physical plant, but they should enter through explicit authority gates. \Experimental\quad Current social-roam autonomy consumes fresh bounded lidar scans and, near 82 percent of its configured activation radius, attempts to turn and drive toward its origin. Destination autonomy adds bounded routing and further stale-sensor gates; neither behavior is a physical geofence or proof of containment. Optional Gemini tools declare high-level action proposals, but most do not yet have executors. None of these paths should bypass local validation, the control-master state, the Arduino deadline, or the physical emergency stop.

\begin{advancedbox}
Document safety as a control hierarchy, not a list of features. For every hazard, name the prevention, detection, mitigation, independent stop, diagnostic coverage, proof test, and residual risk. Ordinary cameras, IR sensors, and software timers may support risk reduction but are not automatically safety-rated.
\end{advancedbox}

\ROBPlaceholder{Create an author-approved current block diagram showing installed batteries, disconnect and protection, DC buses, converters, Mac mini, any Raspberry Pi units, the Arduino-compatible base controller with exact model, motor controllers, Maestro, Amber arm controllers, cameras, lidar host, router, and every external port. Mark planned and removed hardware separately.}

\part{Mechanical build}

\ROBChapterPhoto{Chapter 3}{Chassis, treads, and drivetrain}{2024-nearly-assembled-base.jpg}

\chapter{Build the mobile base}

The base is the foundation for every later system. Presentation pages 3-9 show left and right wooden side templates, motor openings, wheel mounts, a battery opening, and the desire to reproduce the form in black powder-coated aluminum. Pages 10-20 document the tread tensioner, wheels, sprockets, and shaft collars. Later gallery photographs show extensive metal and wiring revisions.

The purchased SuperDroid Robots LT2 system is the governing mechanical interface. The vendor's current support material describes the LT2 as a mid-sized, chain-driven skid-steer platform and explains that flipper arms can lift the nose, stabilize the platform, help it climb, and right it after a rollover. Current catalog dimensions and options may not match ROB's purchased revision; retain the invoice, serial/revision evidence, and as-received measurements with the CAD.

\section{Start from interfaces}

Before drawing the final plate, freeze or explicitly version these interfaces:

\begin{itemize}
  \item tread-frame mounting points and full motion envelope;
  \item drive and idler shaft positions, diameters, retention, and bearing supports;
  \item motor face, shaft, and cable clearances;
  \item chain line, sprocket sizes, guard volume, and service access;
  \item flipper shaft, torque path, rear/front location, and end stops;
  \item battery envelope, restraint, protection access, and removal path;
  \item actuator mounts and full extension/retraction envelope;
  \item lifting, tie-down, stand, cover, and inspection points.
\end{itemize}

\ROBPhotoTwo{2022-drive-assembly-rear.jpg}{A centered rear view shows the drive assembly between two treads.}{2024-sprocket-machined-opening.jpg}{A later sprocket view shows why chain-line and plate clearance need dimensioned control.}

\section{Tread tension and support}

The presentation describes aluminum bars with threaded holes that pull the tread assembly outward, free-spinning wheels retained on shafts, and a heavy-load problem that led to improvised HDPE and skateboard-bearing supports. These are valuable design clues but not a final service specification.

Record a repeatable tension method. A statement such as ``tight enough'' cannot be audited. Depending on the tread system, the specification might be deflection under a known force, tensioner position, exposed thread length, torque plus verification, or the manufacturer's procedure.

\ROBPhotoTwo{2022-chain-sprocket-interior.jpg}{A large sprocket and chain are visible inside the base.}{2024-chain-drive-closeup.jpg}{The close chain path illustrates alignment, abrasion, and guarding concerns.}

\begin{safetybox}
Chains, sprockets, and treads create drawing-in, nip, shear, and entanglement hazards. Disconnect and verify power, block stored motion, prevent unintended rolling, and use fixed guards before hands enter the mechanism.
\end{safetybox}

\section{Flipper revision and third-wheel concept}

Presentation pages 21-26 say the flippers moved from the front wheels to the rear after a design revision. The deck also records a one-degree bar offset, cotter-pin concerns, and a proposed perpendicular third wheel raised by an actuator to reduce tread scrub while turning.

\ROBPhoto[0.54\textheight]{rob-v3-page-23.jpg}{The annotated upside-down prototype on presentation page 23 captures the third-wheel idea and identifies a design revision. It is not a final drawing.}

\Planned\quad Treat the third wheel as a separate design project until the builder supplies the geometry, load path, stability analysis, actuator selection, ground clearance, deployed/retracted sensing, failure response, and test evidence.

The builder's intended stair technique was an undulating sequence: a high-torque flipper lifts part of ROB, the treads advance, and the body changes support state before the next step. Added arms, cameras, computers, inverter, batteries, and structure increased normal force and tread scrub. The present robot can be difficult to pivot on concrete and may need small forward/back steering arcs. Reproduce neither stair climbing nor a zero-radius turn until the test plan includes support polygon, tip margin, flipper current/temperature, tread traction, tread-to-step engagement, cable clearance, observer exclusion, and an independent stop.

\section{Drivetrain experiment ledger}

The builder recalls an early combination operating the motors from 12 V at roughly 80 rpm and 30 W, then trials involving 16-, 32-, 40-, and 45-tooth sprockets. The builder-owned HengDrive curves instead describe 24 V B5685-family variants and 60 W operating points. These are not necessarily contradictions: they may refer to different windings, gearbox stages, controller settings, supply voltage, or measurements. They are kept side by side until the installed units are identified.

For every revision, record both sprockets and which one drives. If $N_d$ is driver teeth and $N_o$ is output teeth, the ideal output-speed multiplier is $N_d/N_o$ and the ideal torque multiplier is $N_o/N_d$ before losses. A larger isolated tooth count does not tell which outcome occurred.

\begin{tabularx}{\textwidth}{>{\robcondensed\bfseries}p{0.75in} p{0.8in} p{0.8in} p{0.7in} X}
\toprule
REV & DRIVER & DRIVEN & BUS & MEASURE AND RECORD\\
\midrule
Historical & 16? & unknown & 12 V? & No-load shaft/tread speed, current, controller setting, surface, mass, and temperature.\\
Trial & 32? & unknown & 12/24 V? & Identify whether speed increased by ratio, voltage, or both.\\
Trial & 40? & unknown & 12/24 V? & Measure turning current and flipper lift margin.\\
Trial & 45? & unknown & 12/24 V? & Record chain line, wrap, tension, guard clearance, and failure reason.\\
\bottomrule
\end{tabularx}

The later 12-to-24 V boost produced an immediate observed improvement in speed and available effort. Voltage strongly affects a DC motor's available speed; torque is primarily current-dependent. Do not publish ``doubling voltage doubled torque'' as a general law. Measure bus voltage at the controller, phase/input current as appropriate, converter droop, controller current limit, output speed, force, and temperature under the same load.

\section{Provisional assembly sequence to verify}

\begin{enumerate}
  \item inspect plate dimensions, datum features, threads, deburring, and finish;
  \item install bearings, shaft supports, and fixed structural brackets;
  \item trial-fit tread frames, drive motors, shafts, sprockets, and chains without power;
  \item measure shaft parallelism, sprocket coplanarity, tension range, and full-motion clearance;
  \item install flipper shaft and drivetrain with temporary manual limits;
  \item fit actuator brackets through the full stroke with the load supported independently;
  \item install guarded power components and labeled harnesses only after mechanical inspection;
  \item add batteries using a rated lift and restraint plan;
  \item perform continuity, insulation, polarity, and protective-device checks;
  \item test one actuator at a time on stands under a bounded current and command limit;
  \item install guards and covers before floor testing.
\end{enumerate}

\ROBPlaceholder{Supply final chassis length, width, plate thicknesses and alloys, tread model, wheel and sprocket data, shafts, bearings, collars, chain, tension procedure, flipper dimensions, fastener schedule, torque values, guard drawings, total mass, center of mass, and as-built CAD.}

\ROBChapterPhoto{Chapter 4}{Torso, body, head, and arms}{2025-rob-side-full-body.jpg}

\chapter{Build above the base}

The upper structure must hold cameras and arms while surviving acceleration, arm moments, cable pull, transport, and public interaction. The presentation openly records weak prototype areas: body-to-torso joints pulling on metal, bent L brackets, and wooden parts glued and screwed together.

\section{Body-to-torso connection}

Presentation pages 56-60 show the connection under load and call for mechanical improvement. A final design should provide:

\begin{itemize}
  \item a defined load path into the chassis;
  \item bearing surfaces or pivots sized for radial, axial, and moment loads;
  \item hard stops and motion limits independent of software;
  \item retention that remains safe after one fastener loosens, where practicable;
  \item inspection access and visible wear indicators;
  \item cable routing that does not become the motion stop;
  \item a transport lock that does not depend on powered holding torque.
\end{itemize}

\section{Body rotation and cable continuity}

Presentation page 67 preserves an earlier limited-rotation body-bearing concept. The builder now reports a custom B-COMMAND rotary interface installed inside the stepper plate so 24 V power and USB 3 data can cross the rotating boundary. The initial mechanical order placed the mating terminal geometry on the wrong sides. The builder removed the incompatible plastic top piece and bonded the slip ring into the plate. Treat that as an as-built repair requiring adhesive identification, bond-area inspection, axial/radial retention, runout, strain relief, thermal review, and a service/replacement plan---not as a vendor-approved mounting method.

The installed builder-owned drawing is identified as \texttt{RX-USB001-022-02000C03}. Selected interface facts are: one USB 3.0 path; two 14 AWG PTFE-insulated red/black power conductors; 10 A per power conductor at 24 V; gold-to-gold contacts; IP40; $-20$ to $+60\,^{\circ}\mathrm{C}$; and a mechanical speed rating of \textbf{0--30 rpm}. The builder corrected an earlier recollection of revolutions per second. The drawing gives a typical five-million-revolution life under stated room conditions, but actual life depends on speed, temperature, current, vibration, alignment, and installation.

A second custom drawing, \texttt{RX-ETH001-QS1-02000C04}, describes the intended head interface: USB 3.0, Gigabit Ethernet, and two 5 A/12 V power conductors. Its drawing shows AWG 22 power leads, while the builder recalls AWG 18. Inspect the physical unit and obtain an amended manufacturer record before assigning ampacity or installing it. This second unit is \Planned\ unless the head installation is photographed and continuity-tested.

\begin{safetybox}
The drawing's 0--30 rpm rating is a component maximum, not permission to command the torso at 30 rpm. Human proximity, arm sweep, cable motion, stepper torque, balance, stopping distance, controller voltage, and structure require a far lower verified operating envelope. The builder believes the Pololu stepper controller may tolerate higher supply voltage, but the exact Tic model, motor rating, configuration, and present 12 V supply must be identified before considering 24 or 48 V.
\end{safetybox}

\ROBPhotoTwo{2025-rear-electronics-bay.jpg}{The rear bay shows how upper-body motion can pull on a dense harness.}{2025-labeled-cables.jpg}{Temporary labels aid teardown, but permanent routing must account for repeated flex and abrasion.}

\section{Head and neck}

The archive spans an earlier spherical multicamera head, an Insta360 Pro II concept, OAK-D sensors, a later spherical head, and a 2025 geared neck mechanism. The builder identifies the present panoramic camera as an Insta360 Pro II and the real-time head depth camera as an autofocus OAK-D Pro.

Pan, lower-neck tilt, and upper-neck tilt use Hitec HSB-9380TH servos commanded by the 24-channel Pololu Maestro. Hitec's public specification identifies a 6.0--7.4 V digital brushless servo, a 25-tooth output spline, 34 kg-cm maximum torque at 7.4 V, 0.14 s/60 degrees at 7.4 V, and approximately 2.1 A stall current. The builder recalls a 12-tooth servo gear driving a 92-tooth neck gear. That may describe an attached pinion rather than the servo spline, but it must be measured. A 12:92 external ratio would ideally slow the neck and multiply torque by $92/12\approx7.67$ before losses; it would also multiply backlash and reflected structure loads in ways the servo rating alone cannot certify.

\ROBPhotoTwo{2022-oakd-depth-camera.jpg}{The OAK-D stereo camera is a distinct sensing module, not the whole head design.}{2025-neck-gear-train.jpg}{The 2025 neck gear train exposes stacked gears and support plates that need calibrated limits and guarded pinch points.}

\section{Arms}

The deck proposes two Amber seven-degree-of-freedom arms with force/current-sensing grippers. Current software includes Amber Python/SSH paths, Maestro servo control, and keyframe fields. Photographs show the installed articulated arms and their controller boards. This establishes integration work, not a verified payload or autonomous grasp capability.

\ROBPhotoTwo{2025-detached-articulated-arm.jpg}{A detached arm reveals its many joints and long reach.}{2022-arm-controller-board.jpg}{A circular controller board sits behind the arm mechanism.}

\Commanded\quad Arm and neck values can be issued through current control paths.\par
\Unverified\quad Joint feedback, zeroing, limits, load rating, collision envelope, emergency behavior, and physical outcome were not measured in this review.\par
\Planned\quad General object pickup remains unavailable because camera-to-arm transforms, inverse kinematics, collision checking, and a verified grasp executor are incomplete.

\ROBPlaceholder{Supply upper-body drawings; stepper motor and exact Pololu Tic model/configuration; slip-ring serials and installation photographs; adhesive/retention design; runout and rotation limits; torso/third-wheel actuator roles; head sensor inventory; neck pinion and 92-tooth gear measurements, axes, backlash, limits, and home sensors; arm model/firmware, joint definitions, zeroing, payload, reach, current limits, gripper behavior, collision envelope, cable routing, and transport restraints.}

\ROBChapterPhoto{Chapter 5}{From template to finished part}{2022-drilling-metal-rail.jpg}

\chapter{Fabrication and finish}

The presentation asks how to print and weld the aluminum, whether templates or a 3D scan would help, and whether an outside robotics shop should quote the metalwork. Those questions prove that a complete final fabrication recipe is not present. This chapter supplies the record structure the builder should fill.

\section{Measurement plan}

Choose datums before collecting dimensions. For every critical measurement, record:

\begin{itemize}
  \item feature and datum names;
  \item nominal value and unit;
  \item tolerance or functional clearance;
  \item instrument and resolution;
  \item repeated measurements and uncertainty;
  \item temperature or load state if relevant;
  \item photograph or drawing revision;
  \item who measured and when.
\end{itemize}

\ROBPhotoTwo{2024-caliper-tread-measurement.jpg}{A digital caliper is used across the tread frame; the publication needs the datum and recorded value.}{2022-bracket-fit-check.jpg}{A fit check can reveal interface errors before a hole becomes permanent.}

\section{Process traveler}

For each fabricated part, create a traveler with these gates:

\begin{enumerate}
  \item released drawing and material certificate or stock identification;
  \item saw/cut blank with allowance;
  \item workholding setup and datum pickup;
  \item roughing operation;
  \item intermediate inspection;
  \item finish holes, pockets, threads, bends, or weld preparation;
  \item deburr and edge-break requirement;
  \item weld/braze/bond sequence and distortion check, if applicable;
  \item surface preparation and coating;
  \item final dimensions, photographs, and acceptance signature;
  \item nonconformance and rework record.
\end{enumerate}

\begin{safetybox}
The builder or qualified shop must write the actual machine, cutter, feeds and speeds, workholding, coolant, chip control, guarding, ventilation, hot-work, welding, grinding, coating, and PPE instructions. A generic book cannot make a particular shop operation safe.
\end{safetybox}

\section{Design for assembly and service}

Use captive or accessible fasteners where covers need regular removal. Separate abrasive motion and sharp edges from harnesses. Provide inspection windows for chain line, tension, battery restraints, and protective devices. Ensure a heavy component can be supported before its final fastener is removed. Include lifting points and a stand plan in the CAD, not as an afterthought.

\ROBPlaceholder{Insert the builder-approved machining and assembly travelers, final CAD/template files, CNC or manual process, weld procedure, coating system, adhesives, inspection tools, rejected-part lessons, outside vendors, costs, and photographs paired with each actual operation.}

\part{Power, electronics, and firmware}

\ROBChapterPhoto{Chapter 6}{Power architecture}{2024-batteries-and-power-leads.jpg}

\chapter{Store, convert, protect, and disconnect energy}

\section{What the archive says}

The builder reports four 20 Ah LiFePO$_4$ batteries and refers to the bank as 80 Ah. That arithmetic describes a compatible parallel capacity, but a safe parallel topology is not established until pack voltage, chemistry, BMS behavior, state-of-charge matching, branch protection, conductor resistance, and manufacturer permission are verified. Later photographs show only part of the physical arrangement and cannot prove the hidden connections.

The current narrative says all battery negative leads meet at a negative bus. Positive leads travel to the front on/off switch and then to a positive 12 V bus bar. A large converter boosts the base supply to 24 V for the motors and upper-body feed. Behind the belly are separate 12 V and 24 V buses. A 24-to-12 V DC converter supplies selected body loads and the 12 V input path for the fanless sine-wave inverter. A 24-to-48 V boost stage supplies the AMBER B1 arms through the two cutoff switches behind the arms. These statements are builder-reported and must be reconciled against an as-built single-line drawing and continuity test.

\ROBPowerArchitectureDiagram

\ROBPhotoTwo{rob-v3-page-41.jpg}{Historical 2021--2022 presentation concept: page 41 sketches four batteries and packaging around the treads; it is not evidence of the current power topology.}{2024-batteries-and-power-leads.jpg}{The 2024 photograph shows the physical packs and leads visible in that revision.}

\section{Required single-line diagram}

The final manual needs an electrical single-line diagram from every energy source to every load. At minimum, identify:

\begin{itemize}
  \item battery pack chemistry, nominal and full-charge voltage, capacity, BMS, and series/parallel connection;
  \item main fuse or breaker near each energy source;
  \item main disconnect and emergency-stop contactor/controller path;
  \item precharge, inrush, anti-spark, or safe connector sequence where required;
  \item 12 V, 24 V, 5 V, and other buses with converter ratings and derating;
  \item tread, flipper, actuator, arm, compute, sensor, light, audio, router, and accessory branches;
  \item wire gauge, insulation rating, connector, polarity, routing, and strain relief;
  \item grounding, chassis bonding, shielding, and separation of noisy and sensitive returns;
  \item charger interlock, shore-power protection, inverter isolation, and exposed receptacles;
  \item test points, indicators, and safe discharge behavior.
\end{itemize}

\ROBPhotoTwo{2024-power-cable-pass-through.jpg}{A pass-through needs abrasion protection and restraint.}{2024-voltage-gauge.jpg}{A 13.1 V display is a diagnostic clue, not proof of state of charge or system health.}

\section{Loads named in the builder record}

\begin{tabularx}{\textwidth}{>{\robcondensed\bfseries}p{1.1in} p{1.7in} X}
\toprule
DOMAIN & REPORTED LOADS & REQUIRED VERIFICATION\\
\midrule
Base 12 V & tread/flipper controllers before 24 V revision, linear-actuator controller, Arduino/base hub, onboard charger connection & Present motor feed, branch fuses, current limits, charger interlock, return topology.\\
Base/body 24 V & HengDrive motors after boost, rotary power path, torso bus, stepper candidate, Maestro/peripherals as configured & Converter make/model, 240 W recollection, continuous/surge current, cooling, slip-ring current sharing, voltage drop.\\
Body 12 V & backpack router, camera charging, AMBER Ubuntu computer path as recalled, Mac/inverter-related loads & Exact converter and each device input voltage; do not call a DC converter an inverter.\\
120 VAC output & fanless pure-sine inverter, recalled 150 W continuous / 300 W peak, used for AC-powered compute/accessories & Model, grounding/isolation, receptacle protection, inrush, shutdown, enclosure, shore-power separation.\\
Arm 48 V & two AMBER B1 arms through rear cutoff switches & Converter rating, wiring, branch protection, connector, cutoff contact rating and whether it interrupts both commanded and fault energy states.\\
\bottomrule
\end{tabularx}

The onboard NOCO Genius charger is recalled as a 2 A-class unit. It is a fallback charging path, not evidence that ROB can operate indefinitely while plugged in. At 2 A, even an ideal 80 Ah bank requires on the order of 40 hours to replace 80 Ah before taper, losses, cell balancing, and concurrent loads. Record the exact charger model and approved battery topology. Interlock motion and hazardous charging/service states according to the electrical design.

\section{Motor electrical evidence}

The motor label visible in build photographs identifies a HengDrive B5685G family. Builder-owned outline drawings identify a rated 24 V family with an 8--26.4 V operating range and five leads: red power positive, black power negative, green speed output, yellow direction, and blue PWM speed command. They show gearbox options including 1:22.6 and 1:276. Separate 24 V performance curves describe 60 W operating points at different motor speeds. File naming around ratio and speed is ambiguous enough that this manual does not assign either curve to an installed tread or flipper motor.

The required identification test is non-destructive and staged: photograph every label and connector, record winding/gearbox/serial markings, trace each five-wire harness without energizing, compare controller configuration, then measure bounded no-load output speed at the shaft and tread. Only after current limiting, direction, brake, tach level, and stop are known should a loaded test record output speed, input current, converter voltage sag, temperature, and force.

\begin{advancedbox}
A builder-recalled 240 W converter corresponds to 10 A output at 24 V before losses. The 12 V input would require more than 20 A at full output after conversion loss. This does not approve or reject the circuit; it explains why both input and output conductors, fuses, connectors, cooling, battery sag, and simultaneous motor demand require measurement.
\end{advancedbox}

\section{Emergency stop and safe energy state}

A physical E-stop should be independent of the UI and ordinary network. Its design must follow the actual hazard analysis: which energy is removed, which holding brakes remain, whether abrupt removal creates a worse hazard, how reset works, how contact welding or controller failure is detected, and how the operator proves it before a show.

\ROBPhoto{2025-emergency-stop-bench-layout.jpg}{A yellow emergency-stop button and control boxes appear in a 2025 bench layout. The photograph does not reveal contacts, safety category, or final wiring.}

\ROBPlaceholder{Provide the authoritative as-built schematic and calculations: pack make/model/serial and topology, BMS compatibility, per-pack and main protection, NOCO charger model, disconnect and E-stop circuit/contact ratings, converter and inverter models, branch currents, wire gauges, connectors, grounding/bonding, shore-power isolation, runtime, inrush, thermal measurements, voltage-drop map, and open/short/reversed-polarity fault tests.}

\ROBChapterPhoto{Chapter 7}{Arduino base controller}{2024-dense-internal-routing.jpg}

\chapter{Base electronics and firmware}
\ROBSourceNow{ROBArduino/ROBOT_CEREBELLULAR_BASE_APP/ROBOT_CEREBELLULAR_BASE_APP.ino}{This is the current firmware reference for the entire chapter. Torso and Head sketches are cited only where their retired behavior is being compared.}

\section{Three historical roles, one present role}

The \texttt{ROBArduino} archive resolves an important architecture question. Earlier ROB revisions had three named Arduino sketches, but the builder reports that present-day ROB uses only the Base code.

\begin{tabularx}{\textwidth}{>{\robcondensed\bfseries}p{0.72in} p{1.45in} X}
\toprule
ROLE & PUBLICATION STATUS & INSPECTED SOURCE BEHAVIOR\\
\midrule
Base & \Implemented\ current role reported & Seven-field tread/flipper/actuator command; IR and MPU9250 processing; USB host link and actuator SoftwareSerial. The exact flashed binary and board remain to be recorded.\\
Torso & \Legacy\ retired & Fourteen four-character Maestro targets for channels 0, 1, and 6--17; MPU9250; disabled GPS path. Its timeout actuation is commented out. Two archived filenames contain identical bytes.\\
Head & \Legacy\ retired & MPU9250 and PIR experiment with disabled GPS declarations. The active parser consumes only one copied legacy field, not the Base seven-field command; timeout stop actuation is commented out.\\
\bottomrule
\end{tabularx}

Current diagrams, startup procedures, and hardware counts must show one Base Arduino. Historical diagrams may show all three only when labeled with their revision context. Cerebro still contains serial names and helpers for Base, Torso, Head, and Maestro; those software paths are evidence of compatibility history, not proof that three Arduino boards remain installed.

\section{Confirmed pin and communication map}

The builder identifies the installed controller as an Arduino Mega 2560. Arduino's official specification gives 54 digital I/O pins, 15 PWM outputs, 16 analog inputs, and four hardware UARTs; the remembered ``55 digital I/O'' count is corrected here. The exact board revision, core, library versions, flashed binary/hash, and upload date remain to be supplied.

\ROBArduinoWiringDiagram

\begin{tabularx}{\textwidth}{>{\robcondensed\bfseries}p{1.38in} >{\centering\arraybackslash}p{0.7in} >{\centering\arraybackslash}p{0.7in} >{\centering\arraybackslash}p{0.78in} X}
\toprule
LOAD & SPEED & BRAKE & DIRECTION & NOTES\\
\midrule
Right tread & D2 & D3 & D25 & Active-low speed mapping in firmware\\
Left tread & D4 & D5 & D27 & Mirrored direction polarity\\
Flipper & D6 & D7 & D29 & No implemented limit input\\
\bottomrule
\end{tabularx}

\begin{tabularx}{\textwidth}{>{\robcondensed\bfseries}p{1.55in} p{1.2in} X}
\toprule
FUNCTION & CONNECTION & CURRENT SOURCE BEHAVIOR\\
\midrule
Mac base serial & USB, 250,000 baud & Fixed-width command plus diagnostic text and an existing role-specific startup line\\
Actuator controller & SoftwareSerial D22/D23, 19,200 baud & Compact serial forward/reverse; nominal speed -3200 to +3200; special inputs +3201 and -3201 clear safe start before commanding the corresponding 3200 magnitude\\
IR array & A5 through A0 & Six SharpIR instances; front/back warning thresholds at 25 cm; numeric six-sensor printout is commented out\\
IMU & I2C via Wire; D20/D21 if Mega & MPU9250 initialization and orientation telemetry\\
Speed feedback & Proposed D18/D19/D20 & Commented TODOs disagree: the header calls D18 left and D19 right, while later pin declarations reverse them; D20 conflicts with Mega I2C SDA\\
\bottomrule
\end{tabularx}

Cerebro consumes the existing \texttt{WARNING! FRONT}, \texttt{WARNING! BACK}, and blocking-error sentences without requiring a firmware flash. Its SceneKit diagnostic highlights the corresponding pair for three seconds. Silence becomes ``clearance unknown,'' not ``path clear,'' because the legacy stream has neither explicit clear events nor numeric distances. These terrain-sensitive IR warnings do not replace RPLidar traversability or a physical stop.

The existing Base sketch emits \texttt{BEGIN BASE STARTUP SEQUENCE} after IMU initialization. Cerebro pulses DTR to restart each USB serial candidate, waits up to 15 seconds for that exact role-specific line, retains the matching descriptor, and reruns the scan when the operator refreshes. It transmits no command bytes and requires no firmware flash. This removes reliance on a hub-dependent \texttt{/dev/cu.*} name, but it is role discovery rather than cryptographic authentication or hardware verification.

\section{Serial frame}

\begin{center}
\begin{tcolorbox}[colback=ROBInk,colframe=ROBAccent,width=0.96\textwidth,arc=0mm]
{\ttfamily\color{white}\large
\textcolor{ROBAccent}{\textasciitilde}+0000,+0100,+0000,+0100,+0000,+0000,+0000}
\end{tcolorbox}
\end{center}

The 42 ASCII bytes represent left brake, left signed speed, right brake, right signed speed, flipper brake, flipper signed speed, and actuator signed speed. Brake field 0 maps to HIGH and is described as released; nonzero maps to LOW and is described as engaged. Tread and flipper speed output is \texttt{255 - abs(command)}, consistent with the active-low motor control concept. The physical brake electronics are not documented by the five-wire motor outline.

\section{Actuator compact serial}

The code sends 0x83 to exit safe start, 0x85 for forward, and 0x86 for reverse. It encodes magnitude in a five-bit low chunk and remaining high bits. Special input values +3201 and -3201 also clear safe start before becoming +3200 or -3200. Reset should be separated from speed in a future typed protocol.

\section{IR and IMU status}

\Experimental\quad Six IR sensors are constructed and front/back readings are sampled. After ten repeated close readings, the code prints a warning. The two lines that assert movement-inhibit flags are commented out, side sensors do not participate, and full six-value telemetry is commented. The builder reports grass/floor false triggers.\par

\Implemented\quad MPU9250 acceleration, gyro, and magnetometer readings feed a Mahony quaternion filter and print yaw, pitch, roll, and temperature. The orientation is telemetry only; tilt does not inhibit motion. The yaw correction is hard-coded for an older Austin, Texas declination and must not be treated as current calibration.

\section{Safety and correctness audit}

The historical firmware should be preserved as an artifact, then replaced behind fixtures. Priority defects include:

\begin{enumerate}
  \item the five-byte numeric buffer lacks a C-string null terminator before \texttt{atoi} and debug printing;
  \item one available byte triggers 41 further reads without proof of a complete frame;
  \item separator positions, signs, ranges, checksum, and frame completeness are not validated;
  \item an extra read after the seventh field can consume the next frame's start marker;
  \item any received byte refreshes the deadman before frame validation;
  \item the deadman depends on loop counts rather than elapsed monotonic time;
  \item timeout sends zero speed with brake fields that release brakes;
  \item IR inhibit assignments are disabled and would not update outputs until another command path runs;
  \item one rotation test duplicates the same condition and omits the mirrored turn;
  \item command values are not clamped to PWM and actuator ranges;
  \item ramp constants, encoder feedback, and voltage monitoring are TODO-only;
  \item brake, direction, and PWM update ordering has not been proven jerk-free;
  \item the flipper and actuator have no firmware-visible end stops, position feedback, or motion timeout;
  \item setup writes the inactive value 255 to both tread PWM pins but never initializes flipper PWM D6; after \texttt{pinMode(OUTPUT)}, its default LOW is the maximum-command level under the active-low mapping, so boot safety relies on the unverified flipper-brake path;
  \item IMU initialization can abort before motor outputs are explicitly initialized safe;
  \item direction method names, comments, and debug strings disagree.
\end{enumerate}

\begin{safetybox}
Do not ``clean up'' direction logic on the live robot by inspection. First capture protocol fixtures, lift and guard the drivetrain, limit current and speed, verify every brake and direction state, measure timeout behavior, and retain an immediate independent stop.
\end{safetybox}

\section{Replacement-firmware architecture}

A modern base controller should contain:

\begin{itemize}
  \item safe GPIO initialization before optional sensors;
  \item bounded ring buffer and explicit framed decoder;
  \item version, length, type, sequence, range, and checksum validation;
  \item atomic command acceptance only after a complete valid frame;
  \item a monotonic deadline refreshed only by a valid authorized command;
  \item explicit states: boot-safe, disarmed, armed-neutral, moving, stopping, fault, E-stop;
  \item separate commands for reset, arm, drive, flipper, actuator, and diagnostic requests;
  \item clamped outputs, ramp and direction-change interlocks, and defined brake/coast policy;
  \item end-stop, encoder, current, voltage, and temperature feedback where hardware supports it;
  \item structured telemetry with units and status flags;
  \item unit, parser-fuzz, timing, and hardware-in-the-loop tests.
\end{itemize}

\section{Interrupt-first rewire proposal}

The motor controllers expose speed feedback, but the installed code never attaches interrupts. The original map spends external-interrupt-capable D2 and D3 on right-motor outputs, proposes D20 for one tach while I$^2$C uses D20/D21, and uses \texttt{SoftwareSerial} despite spare hardware UARTs. The following is a design proposal, not authorization to move wires:

\begin{tabularx}{\textwidth}{>{\robcondensed\bfseries}p{1.48in} p{1.12in} X}
\toprule
FUNCTION & PROPOSED MEGA PIN & MIGRATION NOTE\\
\midrule
Right / left / flipper tach & D18 / D19 / D2 & All external interrupts; verify voltage, pull-up, edge, pulses/revolution, noise, and maximum rate.\\
Right speed / brake / direction & D8 / D24 / D25 & D8 retains PWM; D24 is ordinary digital output. Prove the controller's active-low speed behavior.\\
Left speed / brake / direction & D4 / D5 / D27 & Preserve current assignments.\\
Flipper speed / brake / direction & D6 / D7 / D29 & Preserve current assignments and add independent end/position protection.\\
LACT controller & Serial2 D16 TX / D17 RX & Replace \texttt{SoftwareSerial}; connect only the required direction(s) with verified 5 V/3.3 V compatibility.\\
IMU I$^2$C & D20 SDA / D21 SCL & Short, local, twisted/ground-referenced routing only; do not reuse D20 as tach.\\
IR & A5 through A0 & Structured telemetry with age/quality; no single terrain-sensitive sample receives stop authority.\\
\bottomrule
\end{tabularx}

Each ISR should read no sensor bus, allocate no memory, print no serial text, and make no motion decision. It should increment an edge counter and capture a timestamp in \texttt{volatile} state. The main loop atomically snapshots those fields and computes speed over elapsed monotonic time. Telemetry should publish pulses, window length, derived speed, age, saturation/overflow, and plausibility. Closed-loop tread control should begin in observation-only mode, compare commanded with measured speed, and retain operator authority during mismatch tuning.

\section{Successor controller study}

The Arduino GIGA R1 WiFi is the leading Arduino-branded research candidate, not a drop-in replacement. Official documentation lists 76 GPIO, 13 PWM pins, four UARTs, a dual-core STM32H747, and an FDCAN controller on D93/D94 that still requires an external transceiver. Its I/O logic is 3.3 V with a 20 mA per-pin maximum and a 140 mA aggregate I/O limit. Every existing 5 V signal must therefore be measured and level-adapted as required.

An initial bench allocation can reserve D2/D3/D18 for three tach inputs, D4/D5/D6 for three PWM outputs, separate ordinary GPIO for brakes/directions, D16/D17 for the actuator UART, and D93/D94 for future isolated CAN. This allocation must pass a board-core-specific \texttt{attachInterrupt(digitalPinToInterrupt(pin), ...)} compile/run test, edge-rate test, timer/PWM interaction test, and safe-reset test before a harness is designed.

The GIGA base board does not include an IMU. The GIGA Display Shield adds a BMI270 over I$^2$C, but a display shield is a poor reason to keep a long noisy sensor bus. Better successors place the IMU near the controller/center-of-motion with short SPI or I$^2$C wiring, isolate it from motor returns, timestamp locally, and send typed orientation/quality telemetry. The existing OAK-D Pro W also contains an IMU, but its frame, latency, USB lifecycle, and mounting role differ from a base safety/control sensor.

\ROBPlaceholder{Confirm Mega revision and core; installed libraries and firmware hash; every connector and logic voltage; brake circuit; motor/flipper polarity; allowed command ranges; measured deadman deadline; brake/coast behavior; tach electrical interface/pulses per revolution; IR models/positions/calibration; IMU orientation and failed long-wire installation; actuator controller, end stops, current limits, and safe state. Approve or reject the proposed Mega/GIGA maps through a signed design review.}

\part{Software and perception}

\ROBChapterPhoto{Chapter 8}{Cerebro on macOS}{2022-fanless-computer-and-serial.jpg}

\chapter{Central coordination on the Mac mini}
\ROBSourceNow{Cerebro/Cerebro/ROBMainViewController.mm}{This chapter begins at Cerebro's coordinator, then names each helper source as ownership crosses into serial, camera, autonomy, AI, or animation code.}

Cerebro is a mixed Swift, Objective-C, and Objective-C++ macOS application. Its central view/controller layer coordinates UI state while specialized components own serial, camera, speech, autonomy, identity, private conversation, recording, security, and animation.

\section{Principal components}

\begin{tabularx}{\textwidth}{>{\ttfamily\scriptsize}p{2.35in} X}
\toprule
SOURCE COMPONENT & RESPONSIBILITY\\
\midrule
ROBMainViewController.mm & Operator UI, controller messages, action/autonomy surfaces, subsystem coordination.\\
ROBSerialBox.m / .h & Present Base serial ownership and heartbeat rendering, plus legacy discovery/helpers named for retired Torso and Head Arduino roles and Maestro hardware.\\
CameraManager.swift / ROBInsta360* & Separate face/belly/panorama providers, frame-set ownership, bounded delivery, helper and demand lifecycle.\\
CameraViewController.swift & Vision processing and camera presentation.\\
ROBSpeechBox.m & Local speech recognition configuration and speech synthesis.\\
ROBAutonomyCoordinator.swift & Bounded session state and deterministic local autonomy coordination.\\
ROBAI.swift & Optional multimodal Gemini session and high-level tool coordination.\\
ROBMessages* & Fail-closed one-to-one bridge, responder, bounded attachments/current information, optional encrypted transcript memory, and local administrator-command policy.\\
ROBFace* & Consent enrollment, encrypted model-tagged gallery, selectable embedding inference, open-set/temporal recognition, and identity window.\\
ROBRecordingCoordinator.swift & Explicit recoverable synchronized training sessions and the rule that autonomous commands cannot self-label.\\
GeminiRoboticsProtocol.swift & Versioned messages and validation for model-facing robot actions.\\
KeyframeAnimationManager.swift & JSON-persisted animation values for arms, head/neck, treads, flipper, actuator, torso, and speech.\\
AutoNet shared/transport files & ROBControl v2 framing, pairing, roles, session identity, and authorization.\\
\bottomrule
\end{tabularx}

\section{Present belly compute, USB, and Ethernet record}

The builder reports three Mac mini generations: Intel, M1, and the present M4. The M4 runs Cerebro. A separate AMBER Robotics Ubuntu computer owns the vendor kinematic/core environment and the two arm CAN paths. A backpack router joins the home Orbi Wi-Fi network, and a Netgear switch expands wired connectivity for the Mac mini, AMBER box, and Insta360 Pro II.

The M4 presently uses two USB-C hubs plus a USB-A fan-out. Named USB loads include the Pololu stepper controller, 24-channel Maestro, Hecate USB/Bluetooth speakers, keyboard dongle, depth cameras, and the rotary/base chain that reaches the Arduino and the RPLidar USB--Ethernet adapter. The builder reports hub power shutdowns. A connector extension can solve reach or gender, but it cannot solve current, inrush, voltage drop, or signal integrity.

\ROBUSBNETDiagram

Create two inventories from the running M4: a physical port photograph with both cable ends labeled, and a machine-readable USB tree captured at idle, camera startup, video streaming, arm service start, and full sensor load. For each hub record its upstream link speed, power supply rating, per-port limit, negotiated device speed, measured bus voltage, and shutdown log. Replace passive or bus-powered stages with approved externally powered hubs only after grounding, backfeed, isolation, and shutdown behavior are reviewed.

Other named body loads are a conference microphone, a digitally controlled bicycle lamp whose button interface has not been successfully automated, and a fanless pure-sine inverter recalled as 150 W continuous/300 W peak. The lamp remains manual until its button protocol is measured and an isolated, reversible interface is designed; never probe it from a high-current robot bus by trial and error.

\section{Base heartbeat ownership}

Cerebro evaluates controller snapshots on a timer running at about 10 Hz and treats a snapshot as stale once its local receipt age exceeds 0.6 seconds. On the first transition from fresh to stale, it writes one neutral/braked serial frame, stops USB refresh, releases control authority, and allows the Arduino heartbeat to expire. This is a time-based gate, not a fixed count of three missed messages. The layered behavior is stronger than either deadline alone, but the Arduino's own elapsed time and physical stop result still require measurement.

\section{ROBSerialBox as an integration history}
\ROBSourceNow{Cerebro/Cerebro/ROBSerialBox.m}{Read \texttt{initialize\_connection}, \texttt{usbSerialPortPaths}, \texttt{connectToDetectedBase}, \texttt{probeBaseFirmwareAtPath}, \texttt{consumeBaseSerialBytes}, the Vision neck/torso methods, and \texttt{stopBaseMotionAndDropHeartbeat} together.}

\texttt{ROBSerialBox} is the current physical-I/O façade and an archaeological record. It contains Base, Maestro, Tic, AMBER, controller, and retired Head/Torso surfaces. At startup every POSIX descriptor receives the closed sentinel $-1$. Base discovery enumerates only USB callout devices, opens candidates at 250,000 baud, pulses DTR, sends no probe command bytes, and passively waits up to 15 seconds for the already-flashed firmware line \texttt{BEGIN BASE STARTUP SEQUENCE}. Hub-dependent \texttt{/dev/cu.*} names therefore need not be stored. The banner identifies a firmware role; it does not authenticate the board or verify wiring.

Serial reads are arbitrary byte chunks, not messages. Cerebro accumulates bounded data, extracts newline-delimited records, and then interprets them. It recognizes the existing front/back IR warning sentences and a bounded six-value numeric form when available. Legacy warning silence is reported as clearance unknown. Maestro selection instead uses USB vendor/product/interface identity, while the waist Tic path uses preflighted \texttt{ticcmd}; device discovery is protocol-specific rather than ``first port wins.''

The next refactor should retain the existing Objective-C façade for Interface Builder while moving each descriptor, read loop, write queue, reconnect generation, and parser into a single-owner component. Checking \texttt{fd != -1} cannot by itself make a concurrent close/write atomic. This is the practical lesson behind historical \texttt{Bad file descriptor} floods.

\section{External helpers and dependencies}

The OAK-D path uses a supervised Python helper rather than loading DepthAI directly into the control process. Cerebro can select or create a Python environment and validate the required import. Optional Amber scripts and tools such as \texttt{sshpass} and \texttt{ticcmd} are preflighted; missing dependencies disable their feature instead of terminating the app.

\begin{advancedbox}
Subprocess boundaries create explicit failure and update surfaces. The parent must validate executable paths, avoid blocking reads, bound restart backoff, authenticate local IPC, limit message sizes, and make helper unavailability visible. A helper crash should never preserve motion authority.
\end{advancedbox}

\section{Legacy boundary}

\Legacy\quad Historical controller data uses a fourteen-line keyed payload inside the newer authenticated transport. New domain code should not spread that format. A narrow adapter should translate modern typed commands into the legacy application payload until a deliberate migration replaces it.

A malformed ROBControl header or frame can be rejected before application dispatch. A well-formed, authenticated envelope can still contain a malformed legacy controller payload, so the application adapter must validate that payload separately; transport authentication does not make its contents physically safe.

\section{Typed scene state and local intelligence}
\ROBSourceNow{Cerebro/Cerebro/ROBSceneSnapshot.swift}{Compare the latest-value snapshot store with \texttt{ROBMLXRuntime.swift}, \texttt{ROBMLXImprovisationProvider.swift}, and \texttt{ROBAI.swift}. None of these types is a real-time motor controller.}

\texttt{SceneSnapshot} is the consolidation boundary for people, objects, gestures, depth/lidar free-space regions, arm joint pose, reverse camera pose, camera quality, timestamps, and confidence. Missing observations remain missing. Fresh lidar is retained only briefly; aligned depth is sampled in left/forward/right bands; language serialization labels the JSON as untrusted sensor data rather than instructions. A macOS 26 Foundation Models interpreter may return a typed high-level \texttt{AssistantIntent}, but not servo or tread values.

The local \texttt{ROBMLXEngine} actor owns selected model containers, loading, diagnostics, sampled VLM observations, embeddings, and a process-local semantic store bounded to 200 entries. The inspected initial models are a four-bit Llama 3.2 1B instruct model, a four-bit Qwen2-VL 2B vision-language model, and a small GTE embedding model. The informal term ``VLX'' in project conversation refers to this MLX VLM path, not a separate framework. Camera admission is sampled on a multi-second interval and returns observation text only. It never calls motor code. This general semantic store is distinct from the persistent encrypted, purpose-limited face gallery and optional per-sender Messages transcript store.

Local improvisation must decode as one exact versioned JSON plan with allow-listed beat and delivery values. Gemini remains an optional cloud path with separately effective connection, microphone, camera, and action-tool switches. Both providers produce bounded proposals; deterministic policy, operator authority, and physical executors remain separate.

\ROBPlaceholder{Tag the publication source; provide target macOS and hardware; identify enabled optional dependencies and versions; capture redacted current UI screenshots; document app startup/shutdown, serial-device names, selected Python runtime, and recovery after each subsystem fails.}

\ROBChapterPhoto{Chapter 9}{Controllers and secure transport}{2024-open-tracked-base.jpg}

\chapter{Control from iPhone, Watch, and Vision Pro}
\ROBSourceNow{ROBController/Consciousness/AutoNetClient/AutoNetDataTransferProtocol.swift}{The client control contract is the anchor. Compare it with Cerebro's matching \texttt{AutoNetDataTransferProtocol.swift}; the sending UI is in \texttt{ConsciousViewController.mm}, Watch relay in its Watch app directory, and spatial client under \texttt{ROBControllerVision}.}

\section{Controller roles}

The iPhone/iPad ROBController captures manual controls and presents Map, Controls, Auto, Settings, push-to-talk, pairing, destination autonomy, and action-approval surfaces. Its active legacy snapshot carries raw joystick coordinates; it does not use the Vision controller's normalized linear/angular representation. Apple Watch sends voice or joystick intent through the phone and keeps no robot credential. ROBControllerVision provides a SwiftUI/Swift 6 architecture with a deterministic simulator, normalized motion, a 250 ms lease, a latched software E-stop, independent arm control, secure Cerebro control, three H.264 feeds, and an immersive 360 view.

\section{Explicit drive ownership}

\Implemented\quad The latest authenticated operator to request control wins. Cerebro first stops the prior owner's motion, records the new owner, and broadcasts authoritative ownership. Release Control brakes ROB and returns ownership to Cerebro. Each phone/tablet and Vision Pro needs its own pairing credential. Authentication proves the device; ownership says which authenticated device may currently drive.

Camera subscriptions, face recognition, Messages identity, arm authority, and AI approval do not imply drive ownership. Test simultaneous requests, takeover under nonzero input, prior-client replay, disconnect, revocation, and release; every transition must converge through an explicit stop.

\section{Independent Vision arm authority}

\Experimental\quad The current \texttt{rob-arm-control/2} path gives the left and right AMBER arms independent measured feedback, authority UUIDs, leases, selected joints, drafts, in-flight targets, and hold results. Vision never activates an arm or changes its actuator mode. Fresh feedback (no more than 250 ms old), all-seven-joint position mode, exact bounds, a measured baseline, and a Cerebro grant are preconditions.

On-screen lanes and paired PSVR Sense thumbsticks limit each segment to 0.08 radian and average requested speed to 0.20 radian/s. With both arm authorities active, both vertical sticks may jog their matching selected joints concurrently after a 0.15 dead zone. Both grips form one dead-man; releasing either asks both arms to hold. Disconnect, stale input/telemetry, preflight loss, target failure, scene loss, stop, disarm, or software E-stop takes the priority-hold-both path and clears local latches. This is joint jogging, not tracked-pose IK; HIL verification remains pending.

\section{ROBControl v2}

\Implemented\quad The production control path advertises \texttt{\_robctl.\_udp}, negotiates \texttt{robctl/2}, uses TLS 1.3 over QUIC, pins Cerebro's certificate, and performs a reciprocal HMAC-SHA-256 proof with a per-device 256-bit secret. Its fixed 40-byte RCTL header carries version, kind, flags, channel, bounded payload length, monotonic sequence, and message ID.

Validation is layered rather than identical for every message. Transport framing checks applicable bounds, role, session, and sequence rules. Lidar telemetry adds capture-time freshness, while controller freshness is measured from Cerebro's local receipt time. The legacy fourteen-line application payload still requires its own decoder validation.

Pairing creates a fresh device UUID and secret for one named device. The server registry remains authoritative for role and revocation. Bonjour advertises non-secret routing facts, not trust. Each device must receive its own credential. The pinned server-certificate fingerprint is a public identity value rather than a secret; security depends on protecting the device secret and private key.

\begin{tabularx}{\textwidth}{>{\robcondensed\bfseries\color{ROBAccent}}p{1.48in} X X}
\toprule
ROLE & MAY & MUST NOT\\
\midrule
operatorController & Send established control/action/autonomy messages and receive results. & Publish lidar as a trusted sensor role.\\
lidarPublisher & Send bounded typed scan and map telemetry. & Request motion, authorize autonomy, or receive generic controller traffic.\\
\bottomrule
\end{tabularx}

\section{Separate video service}

\Implemented\quad Cerebro advertises \texttt{\_robvideo.\_udp} with \texttt{robvideo/1}. Independent \texttt{front}, \texttt{belly}, and \texttt{insta360} H.264 AVCC feeds use distinct bounded pipelines. Front/belly publish up to 960 by 540; panorama uses 960 by 480; all are capped at 20 fps and 1.5 Mbit/s on ordered reliable QUIC streams. Video has separate framing, queues, authentication domain, and backpressure so media cannot queue in front of a stop command.

Video authorization is bound to the exact live control-session ID and authenticated controller identity; the client opens its media stream after authentication. Control disconnect or credential revocation closes associated video. Individual feed failure does not close control. A Vision window-to-immersive transition can retain the panorama pipeline while independently braking motion; backgrounding tears it down.

\section{Safety timing layers}

\ROBStopChainDiagram

Current timing points include a 250 ms Vision lease, about 450 ms Watch relay watchdog, about 600 ms Cerebro snapshot freshness, and an Arduino loop-count deadline that must be measured. The physical E-stop remains an independent required layer.

\section{Privacy review}

The current active iPhone path requests Always location authorization and includes live latitude and longitude in each controller snapshot. This is current privacy debt, not merely a historical format detail: remove the fields, minimize their precision and retention, or document a necessary purpose and consent policy before public use. Pairing codes, device secrets, private keys, IP addresses, SSH credentials, API keys, private logs, and location must be excluded from screenshots and sample data. A public certificate or fingerprint may be redacted as operational metadata, but it is not a secret. Each cloud-enabled camera or microphone path needs explicit public notice and consent handling.

\ROBPlaceholder{Document exact controller release versions, supported OS/device matrix, pairing and revocation workflow, operator training, latency and packet-loss results, Wi-Fi/router configuration without secrets, public-location data policy, and current Vision Pro hardware validation.}

\ROBChapterPhoto{Chapter 10}{Advanced H.264 video transport}{2022-oakd-depth-camera.jpg}

\chapter{Encode, transport, decode, and present live video}
\ROBSourceNow{Cerebro/Cerebro/ROBCameraH264Encoder.swift}{Follow one frame through \texttt{ROBVideoServer.swift} and \texttt{ROBVideoProtocol.swift}; then cross repositories into ROBControllerVision's \texttt{H264SampleBufferFactory.swift}, \texttt{H264VideoReceiver.swift}, transport decoder, and display-layer bridge.}

This chapter consolidates the robot-side and Vision Pro books into one advanced three-feed media path. It describes the implemented H.264 profile and mixed-immersive panoramic presentation, not a generic H.264 tutorial and not a promise of stereoscopic spatial video, audio transport, HEVC, or unreliable datagrams. Dataset recording is a separate local subsystem described later.

\section{The end-to-end frame pipeline}

\begin{center}
\begin{tikzpicture}[node distance=0.42cm and 0.24cm,every node/.style={font=\scriptsize}]
\node[draw,rounded corners,fill=ROBCyan!10,minimum width=1.0in,minimum height=0.42in] (capture) {CMSampleBuffer};
\node[draw,rounded corners,fill=ROBAmber!12,right=of capture,minimum width=0.98in,minimum height=0.42in] (scale) {NV12 + letterbox};
\node[draw,rounded corners,fill=ROBAmber!12,right=of scale,minimum width=0.98in,minimum height=0.42in] (encode) {VideoToolbox H.264};
\node[draw,rounded corners,fill=ROBCyan!10,below=of encode,minimum width=0.98in,minimum height=0.42in] (wire) {RVID / RBVD};
\node[draw,rounded corners,fill=ROBCyan!10,left=of wire,minimum width=0.98in,minimum height=0.42in] (validate) {stream validator};
\node[draw,rounded corners,fill=ROBGreen!12,left=of validate,minimum width=1.0in,minimum height=0.42in] (present) {sample renderer};
\draw[->,thick] (capture) -- (scale);
\draw[->,thick] (scale) -- (encode);
\draw[->,thick] (encode) -- (wire);
\draw[->,thick] (wire) -- (validate);
\draw[->,thick] (validate) -- (present);
\end{tikzpicture}
\end{center}

Camera capture produces a Core Media \texttt{CMSampleBuffer} containing a pixel buffer and timing. Cerebro retains only the newest offered raw sample while server work is pending. An accepted, authenticated per-camera subscription creates an encoder at the negotiated even width/height, frame rate, and bitrate. Front/belly ceilings are 960 by 540; Insta360 is 960 by 480; all use 20 frames/s and 1.5 Mbit/s ceilings.

\section{Scale into the encoder's pixel format}

\texttt{ROBCameraH264Encoder} creates a \texttt{VTCompressionSession} whose destination buffers use video-range bi-planar 4:2:0 YCbCr (NV12). A reusable \texttt{VTPixelTransferSession} scales the source with letterboxing so aspect ratio is preserved. The compression-session pool supplies destination buffers; allocation uses a threshold of six so pressure becomes a dropped frame rather than unbounded buffer growth.

At 20 fps, the target interval is 50 ms. Samples arriving sooner are intentionally skipped before conversion. A dropped allocation or compression result requests a future recovery key frame. Every completion object can be claimed once, preventing synchronous drop flags and an asynchronous VideoToolbox callback from delivering the same logical completion twice.

\section{Configure low-latency H.264}

The encoder requests real-time operation, disables frame reordering, prefers constrained baseline profile when supported, supplies expected frame rate and average bitrate, and limits the key-frame interval to approximately one second. Disabling reordering means this initial stream does not depend on B-frames; decode timestamps can remain invalid and presentation order follows the reliable stream.

Each encoded result owns copied bytes rather than a borrowed VideoToolbox buffer. The encoder records a monotonically increasing access-unit sequence, capture time in Unix milliseconds, presentation time and duration in microseconds, synchronization/key-frame state, NAL-length width, SPS/PPS parameter sets when present, and one complete AVCC access-unit payload.

\section{Understand AVCC, SPS, PPS, and access units}

H.264 consists of Network Abstraction Layer (NAL) units. Annex-B streams separate NAL units with start codes such as \texttt{00 00 00 01}. This implementation does not transmit Annex-B. AVCC stores a big-endian byte length before each NAL unit. The receiver must use the negotiated length-field width; it must not search for start-code patterns inside compressed data.

The Sequence Parameter Set (SPS) describes coded properties needed to construct a decoder format. The Picture Parameter Set (PPS) supplies picture-level decoding parameters. Cerebro sends them in a codec-configuration message with a positive generation before access units using that generation. A new configuration, encoder recovery, or requested key frame resends the configuration. An IDR key frame lets the decoder restart without depending on earlier reference pictures.

\begin{advancedbox}
Codec configuration is state, not decoration. A payload can be valid H.264 bytes yet undecodable with the wrong SPS/PPS, NAL-length width, dimensions, stream ID, or generation. Validate all of them before asking Core Media to allocate decoder state.
\end{advancedbox}

\section{Wrap media in two bounded layers}

The outer ordered-stream frame is \texttt{RVID} version 1. Its 32-byte header carries magic, version, header length, message kind, bounded payload length, monotonic connection sequence, and reserved-zero fields. Authentication, capability, subscription, feedback, configuration, access-unit, and end messages receive distinct kinds.

Encoded configuration and access units use an inner \texttt{RBVD} version-1 header of 92 bytes. It binds media to session UUID, subscription UUID, media sequence, capture time, presentation time, duration, timescale, codec-configuration generation, NAL-length width, flags, and payload length. Integers are network byte order. Current hard payload limits are 64 KiB for codec configuration and 2 MiB for one access unit; control JSON is capped at 64 KiB and rejects unknown fields.

\begin{tabularx}{\textwidth}{>{\robcondensed\bfseries\color{ROBAccent}}p{1.2in} p{1.25in} X}
\toprule
BOUNDARY & PRIMARY CHECKS & WHY IT EXISTS\\
\midrule
TLS/QUIC & pinned identity, proof, role & Authentic peer and protected ordered bytes.\\
RVID & magic, version, kind, length, sequence & Incremental connection framing before allocation.\\
RBVD & IDs, codec, timing, generation, lengths & Media belongs to the negotiated live stream.\\
AVCC & NAL lengths and configuration & Compressed payload can be reconstructed safely.\\
Core Media & coded/presentation dimensions & SPS/PPS cannot silently exceed negotiated geometry.\\
\bottomrule
\end{tabularx}

\section{Apply backpressure before latency accumulates}

Video has its own listener, connections, queue, encoder, authentication domain, and send state. It never shares the control message queue. Cerebro permits only one pending raw source frame, one pending encoder output per subscription, and one media send in flight. If encoding or send work is occupied, newer unencoded work is dropped rather than queued. One actual encoded drop or undeliverable output forces a later IDR; replacing a not-yet-encoded raw frame does not break decoder references.

Every RVID send has a ten-second completion deadline. A newly accepted stream has 15 seconds to produce its first frame, and a running stream may not remain silent beyond 15 seconds. A peer that stops reading loses the stream instead of retaining unlimited memory. Three simultaneous maximum-rate feeds can consume roughly 4.5 Mbit/s per viewer plus protocol overhead, so data-plane separation prevents queue coupling but not shared-radio contention.

\section{Decode framing before decoding H.264}

On Vision Pro, the Network.framework adapter first reconstructs complete RVID frames from arbitrary receive chunks. It rejects invalid magic, version, reserved bytes, payload sizes, and ordering before decoding JSON or RBVD. The media decoder then checks session ID, subscription ID, codec, configuration generation, sequence, timing, AVCC length width, and size. These checks occur before creating Core Media state.

\texttt{H264SampleBufferFactory} sorts and extracts SPS/PPS, copies their bytes into temporary stable pointers, and calls \texttt{CMVideoFormatDescriptionCreateFromH264ParameterSets}. It verifies that coded dimensions remain within macroblock rounding of the negotiated size and that clean-aperture presentation dimensions match. This prevents hostile or corrupted parameter sets from expanding the decoder beyond the agreed geometry.

For each accepted access unit, the factory allocates a \texttt{CMBlockBuffer}, copies the bounded AVCC payload, creates timing, and asks Core Media to create a ready sample buffer. The not-sync attachment mirrors the key-frame flag. The display-immediately attachment requests low-latency presentation rather than building a long presentation queue.

\section{Present and recover without blocking control}

The actor-owned \texttt{H264VideoReceiver} feeds an \texttt{AVSampleBufferVideoRenderer} obtained from the UI's display layer. If the renderer is temporarily full, it waits only about 50 ms in short cancellable intervals. Continued pressure drops the access unit and begins recovery rather than accumulating frames.

Sequence gaps, missing configuration, generation mismatch, decoder failure, or \texttt{requiresFlushToResumeDecoding} make the validator require a key frame. Recovery flushes decoder presentation state, rate-limits key-frame feedback to no faster than 500 ms, and discards dependent frames until a valid key frame arrives. A configuration-generation change also flushes before accepting the new decoder state. Statistics report received, rendered, dropped, bytes, last sequence, and key-frame requests at a bounded update rate.

\section*{Timing, reliability, and tradeoffs}

Capture wall-clock time measures end-to-end age. Presentation timestamps provide an internal monotonic media timeline. Duration is derived from the negotiated frame rate. These clocks answer different questions and should not be substituted for one another.

Ordered reliable QUIC simplifies configuration and access-unit ordering, but packet loss can delay later video bytes through head-of-line blocking. Because control uses another QUIC connection, video loss does not queue a stop command, although both flows share Wi-Fi airtime. Future QUIC datagrams would trade retransmission for application-managed loss recovery and require a new negotiated contract; they are not silently enabled by the current enum.

\section{Advanced verification matrix}

Test the media pipeline with synthetic frames before live hardware:

\begin{itemize}
  \item verify scaling, even dimensions, NV12 allocation limits, bitrate, frame-rate skipping, IDR interval, and SPS/PPS extraction;
  \item reject truncated, oversized, mis-versioned, wrong-session, wrong-generation, unknown-field, and out-of-order messages before allocation;
  \item inject access-unit loss and confirm one bounded recovery sequence and key-frame request;
  \item change codec generation and confirm flush, reconfiguration, and key-frame resume;
  \item stall the renderer and network independently and prove memory remains bounded;
  \item close or revoke the control session and prove its video subscription ends;
  \item measure capture-to-display age, encoded size, drops, recovery time, CPU/GPU use, thermal state, and control latency concurrently;
  \item repeat camera unplug, app background, viewer close, and shutdown while a send and encode callback are in flight.
\end{itemize}

Volume 7 explains the Swift/visionOS client composition, three flat feeds, and inward-facing mixed-immersive panorama with -90-degree verified start heading and operator rotation/reset controls. Volume 8 explains Cerebro's broader camera and model scheduling. This chapter is the shared wire-level and Core Media reference.

\ROBChapterPhoto{Chapter 11}{Cameras, depth, lidar, and coordinate frames}{2022-oakd-depth-camera.jpg}

\chapter{Perception and mapping}
\ROBSourceNow{Cerebro/Cerebro/CameraManager.swift}{Camera ownership starts here. Depth helpers remain in Cerebro; lidar transport and fixtures live in the separate RPLidar repository named in the repository source map.}

\section{RGB-D camera}

\Experimental\quad The current camera design runs one pinned DepthAI helper for the face camera and another for the belly camera. Each sends timestamp-synchronized RGB plus depth through its own private local Unix socket. RGB enters the sample-buffer, Vision, Gemini, recording, and video paths. Depth is little-endian UInt16 millimeters aligned to RGB; zero is invalid. AVFoundation remains an RGB-only fallback. A third headless Insta360 service owns camera control, heartbeat, RTMP/ffmpeg preview, demand-driven decode, and six-sector panoramic perception.

The builder identifies an OAK-D Pro Wide in the belly/body and an autofocus OAK-D Pro at the face/head. Record intended role, MXID, socket, product, board, revision, sensor names, calibration, USB speed, focus capability, and firmware because OAK variants differ in color shutter/focus/FOV while sharing similar enclosures. Luxonis documents the RVC2 Pro Wide family with global-shutter monochrome stereo, active IR projection/flood illumination, USB 2/3, and an onboard IMU. Active illumination and full inference can raise peak power; use a reviewed powered path rather than depending on a marginal hub.

Each helper owns exactly one selected OAK exclusively. Old frames are dropped when perception is busy. Missing hardware, wrong role/MXID, malformed IPC, SDK failure, or helper exit becomes per-camera state and bounded retry rather than a control-process crash.

The helper uses DepthAI Camera, Depth, and Sync nodes, requests the selected profile (1280 by 720 default or 640 by 400 for the configurable main camera), aligns RAW16 millimeter depth to RGB, and emits a versioned \texttt{CDP1} packet over a private Unix socket. Cerebro validates requested, observed, and encoded dimensions separately, plus pixel formats, byte counts, and maximum allocation before constructing a CoreVideo buffer. The process boundary contains SDK and USB failure; a future native or XPC helper should preserve the same \texttt{CameraFrameSet} provider contract.

\section{Perception snapshots and bounded rendering}

Camera rate is not inference rate. Preview may consume the newest RGB frame while Vision pose detection, colored depth, point-cloud geometry, Gemini JPEG sampling, MLX VLM sampling, and H.264 encoding run at distinct bounded rates. Slow consumers use latest-value mailboxes. SceneKit reuses or replaces named node roots instead of appending forever; Core Image contexts and pixel-buffer pools are reused; model containers stay actor-owned.

Profile repeated source toggles and 30-minute runs, not one still frame. Look for retained \texttt{CMSampleBuffer} objects, per-frame \texttt{CIContext} construction, ever-growing SceneKit textures or nodes, missing autorelease pools on Objective-C worker threads, duplicate model loads, and repeated diagnostics overwhelming the main thread.

\section{Kinect, OpenNI, NiTE, and PCL archaeology}

\Legacy\quad The \texttt{ROBNiTEManager}, \texttt{TaskControllers}, \texttt{FreenectPCL}, \texttt{k2g}, viewer, shader, serialization, and duplicated ``2'' files preserve an older Kinect/OpenNI/NiTE/PCL depth stack. Archived v2 commits date the first surviving OpenNI/PCL/NiTE sequence to 12 January 2018, with further libfreenect work in May 2018 and continuing perception experiments in the v3 history during 2019. The files entered the fresh v5 Git history in the 2025 import, but were not created by it. They are useful evidence for coordinate and visualization experiments, not the recommended OAK setup.

Before removal, check Xcode target membership, runtime selectors, duplicated model provenance, and any coordinate knowledge that should move into tests or documentation. The modern DepthAI helper improves failure containment by placing the camera SDK and USB ownership outside the control process.

\section{RPLidar}

\Experimental\quad The companion app reads pose, location, laser points, and occupancy maps from Slamware. It publishes scans about every 0.2 seconds and maps about every 5 seconds through typed, sequenced, bounded telemetry. Compact \texttt{RLS1} scans carry a 68-byte header plus four bytes per valid bounded point (8--8192 points, 0.03--30 m); 720 points occupy 2948 bytes, about 14.7 KB/s at 5 Hz. The old sub-meter front/back filter is gone. The occupancy map remains a separate payload. The publisher role cannot drive the robot.

The App Group ID is \texttt{group.com.orbitusrobotics.rob}. Same-Mac publication prefers its Unix-domain IPC with per-sample HMAC, role, revocation, sequence, and freshness checks; paired QUIC is the automatic fallback. This requires matching App Group provisioning. The mapping app aligns updates with scan-captured pose, saves/loads/resets \texttt{.robomap}, and overlays maps on OSM with persisted 50--150 percent scale and -180--180 degree north rotation. North is manual unless a trusted heading source exists.

The RPLidar was a later mechanical stage supported by three 3/8-inch threaded rods. The builder reports Ethernet descending to a base USB hub through a USB--Ethernet adapter; the Arduino uses the same hub for its USB serial path. Confirm the exact RPLidar model because SLAMTEC variants differ between native USB/serial and Ethernet interfaces. Measure the lidar-to-base transform, mast deflection, vibration, rod preload/locking, laser plane clearance, and cable strain through the full flipper, waist, arm, and transport envelopes.

Cerebro checks authenticated publisher identity, monotonic sequence, capture time, kind, and bounds. Stale data is rejected. Social roam and destination autonomy consume only the fresh sensor products their policies name; occupancy/map display never grants motion authority.

\section{IR and IMU}

\Experimental\quad Six Sharp IR sensors are installed. Front/back warnings remain active, but automatic inhibit assignments are disabled and full numeric telemetry is not emitted by the current firmware. Grass repeatedly produced false positives and stopped useful travel during earlier experiments. A future Cerebro policy can consume all six values only after the firmware publishes structured samples with age and quality; filter/debounce them, cross-check lidar and commanded direction, distinguish warning from safety authority, and fail to ``unknown'' on silence.\par

\Experimental\quad The MPU9250 code reports inertial data and derived orientation, but the builder reports that the physical long I$^2$C wires were too noisy inside ROB's high-EMF environment to be useful. It is not a motion-inhibit input. Shorter local wiring, careful return routing, shielding/ground strategy, lower bus speed where appropriate, or a local SPI/CAN sensor node should be tested before restoring the signal.

\section{Coordinate-frame discipline}

Name every transform:

\begin{itemize}
  \item lidar sensor to base;
  \item base to local map;
  \item color camera to depth camera alignment;
  \item camera to head/neck;
  \item head/neck to torso;
  \item torso to each arm base;
  \item arm joint chain to gripper tool frame.
\end{itemize}

Record axis direction, handedness, units, timestamp, calibration method, uncertainty, and valid motion range. A visually plausible transform can still command a gripper to the wrong side of an object.

\ROBPhotoTwo{rob-v3-page-55.jpg}{Presentation page 55 proposes an RPLidar stage above the base.}{2024-open-tracked-base.jpg}{The open base shows how sensors, wiring, compute, and mechanisms become a serviceability problem.}

\ROBPlaceholder{Confirm current camera models, lenses, mounting transforms, USB/Ethernet topology, DepthAI version, exposure and lighting behavior, lidar model/host/address, installed map workflow, IR positions and shrouds, calibration targets, timestamp synchronization, frame conventions, and measured accuracy.}

\ROBChapterPhoto{Chapter 12}{Arms, animation, speech, autonomy, and AI}{2025-detached-articulated-arm.jpg}

\chapter{Behavior above raw motor commands}
\ROBSourceNow{Cerebro/Amber-PythonAPI/Amber V2 API/amber_api/amber_robot.py}{This is the readable high-level AMBER client. Compare its packed command files with the Ubuntu launch JSON and LCM schemas; the captured \texttt{amber\_core} executable is not open source in this snapshot.}

\section{Servo and arm control}

Cerebro uses Maestro servo channels plus AMBER Python paths. ROB's present AMBER evidence describes two seven-joint B1 arms and separate grippers. The Ubuntu-side directories \texttt{L-10} and \texttt{R-11} bind \texttt{can10}/\texttt{can11}, UDP ports 26001/26002, and LCM prefixes \texttt{Left\_}/\texttt{Right\_}. Network clients send UDP or LCM to one core per side; each core, not the client, interfaces with its actuators over CAN.

\ROBCANPhysicalDiagram

The adapter connector must expose or be mapped to CANH, CANL, and the appropriate reference ground. High-speed CAN uses a twisted pair with 120 $\Omega$ termination at the two physical ends; two correct end terminators normally appear as roughly 60 $\Omega$ across CANH--CANL with power removed. Verify whether each USB adapter or arm controller has switchable/internal termination before adding any resistor. ROB has two independent buses, so perform the measurement and side-identity audit separately for \texttt{can10} and \texttt{can11}.

The rear switches behind the arms are reported to interrupt the 48 V arm supply. Document whether each switch removes one arm or a shared converter path, its DC contact rating, arc suppression, connector/fuse order, behavior under regenerative or held loads, and how the operator proves isolation. A switch label does not establish an emergency-stop category.

The checked-in V2 protocol provides status, joint and Cartesian moves, calibration, gripper control, and mode control. Its packed UDP header contains a 16-bit command, 16-bit length, and 32-bit counter. The current client needs stronger validation of source, size, command, counter, freshness, finite values, and authentication before production use. Archived binaries do not include enough source to reconstruct their actuator-level CAN protocol.

The hardened Ubuntu gateway now provides that stronger host boundary: a root-owned initializer maps reviewed USB serials to \texttt{can10}/\texttt{can11}; a systemd service runs unprivileged; LCM status is decoded into locked snapshots; one authenticated controller session owns bounded per-arm queues; heartbeat and leased commands expire; UDP replies are matched by command and counter; and priority hold purges queued work. Volume 6's appended compendium walks through every layer and the service file line by line.

URDF models provide link geometry, joint origins, axes, and limits, but they do not prove ROB's physical mount transforms, encoder zero, payload, safe stopping, or cable collision volume. Measure torso-to-left-base, torso-to-right-base, and each tool transform; then verify forward kinematics over several supported poses before inverse kinematics. Volume 6 contains the protocol tables, joint model, Ubuntu reproduction procedure, and staged dual-arm commissioning gates.

\section{Keyframe animation}

\Implemented\quad \texttt{KeyframeAnimationManager.swift} persists JSON values for arm channels, head/neck, tread movement, flipper, actuator, torso rotation, and speech. Keyframes are reusable data: a sequence can describe expressive behavior independently of the UI that recorded it.

\Commanded\quad Historical servo ``animation'' helpers in \texttt{ROBSerialBox.m} do not yet perform robust smooth interpolation; they update their targets immediately. New interpolation must respect joint rates, synchronization, limits, collisions, and cancellation.

\section{Speech}

\Implemented\quad Cerebro configures an Apple local speech-recognition path and uses local speech synthesis for completed responses. This path reduces network dependence, but it does not describe every microphone use: when the separately optional Gemini session is enabled, microphone audio may be streamed to an external cloud service. Both paths require microphone permissions, public notice, and a plan for unintended capture.

\section{Consent-based face identity}

\Experimental\quad Face identity defaults off and remains local. Enrollment requires a paired, non-revoked operator plus local confirmation and collects 24 varied samples. Vision performs face/landmark/quality gates; recognition adds open-set distance, runner-up margin, and temporal consensus. Opaque UUID profiles encrypt metadata, embeddings, and retained JPEGs with AES-GCM and a device-only Keychain key; whole-profile deletion is available. A recognized name enters untrusted scene context for 15 seconds and never grants motion, shell, secrets, action approval, Messages administration, or any other authority.

Two AdaFace IR18 Core ML encoders are selectable. WebFace4M is the recommended default; VGGFace2 is an optional comparison. Both use 112-by-112 BGR normalized by \texttt{pixel * (2/255) - 1} and return 512-value L2-normalized embeddings. Profiles store their encoder ID and cross-model comparisons are forbidden. The current 0.35 maximum cosine distance and 0.06 margin require on-ROB false-accept/reject, lookalike, lighting, pose, occlusion, and replay calibration. Liveness/depth remains future work. The conversion script validates PyTorch/Core ML agreement and hashes local checkpoints; weights stay out of Git.

\section{Private Messages bridge}

\Experimental\quad The bridge defaults off and uses Full Disk Access for query-only reads of Messages \texttt{chat.db}; Automation permission is needed only to reply. Exact sender allowlists, a persisted high-water mark, at-most-once processing, one-to-one/account checks, and a five-per-minute global rate limit fail closed on groups, outgoing/reaction/partial/stale items, and unexpected accounts.

Each allowed chat has an isolated AI session with no room speech/transcript, motor/action, Music, arbitrary-file, device, camera, or microphone tools. One JPEG/PNG/HEIC/HEIF attachment up to 10 MB/24 MP can be normalized without metadata to at most 2048 px. Gemini may receive it only when configured; otherwise Swift MLX receives pixels and Apple Foundation Models receives bounded textual visual analysis. Fixed-publisher news and Open-Meteo weather with explicit location are allowed; Mac location and caller-supplied URLs are not.

Optional transcript memory defaults off. SQLite fields are AES-GCM encrypted, exact-match indexes use HMAC, and a 256-bit device-only Keychain key plus owner-only permissions protect the store. Retrieval remains bounded to the same canonical sender/account; Gemini use discloses selected text excerpts off-Mac, never stored image pixels. Search, plaintext export, and clear-all controls require an explicit retention/consent policy.

Exact locally configured administrator messages bypass AI and may request Shutdown/Reboot only after exact \texttt{YES} from the same sender/chat within 90 seconds. Confirmation is one-shot. Reviewed scripts run with fixed \texttt{/bin/zsh -f -s}, arrive through stdin, and time out after 30 seconds; message text is never interpolated. Display a critical local warning and never publish real handles/scripts.

\section{Explicit recording and dataset provenance}

\Experimental\quad Recording starts only through an explicit operator action. Recoverable append-only sessions synchronize face/belly RGB keyframes, aligned lossless depth, stereo, calibration, exact \texttt{.rscan} lidar, local pose/odometry, tread/authority, traversability labels, and separate face/belly/Insta360 movies. Requested, observed, and encoded geometry are distinct facts. Autonomous commands are logged but never label themselves; belly traversability becomes a candidate label only after manual traversal plus odometry confirmation. Capacity, consent, retention, deletion/export, dataset splits, and model promotion require human governance.

\section{Controller-activated autonomy}

\Experimental\quad The social-roam profile begins only after explicit controller authorization, captures an activation pose and radius, uses fresh bounded lidar scans, and limits tread speed. Near 82 percent of the configured radius it attempts to return toward its origin; this is not verified containment. Missing or stale lidar requests a motion stop and transitions to \texttt{waitingForLidar} while the autonomy session remains active. Manual takeover, explicit stop, expiry, replacement, or shutdown ends the session. Local deterministic code owns execution.

Controller destination navigation is a distinct deterministic path: ROBController performs Nominatim search, Cerebro uses Valhalla planning, a current pilot limits requests to 50 m, and stale pose/map/lidar/depth/authority gates reject or stop the route. Myriad X sidewalk segmentation provides bounded centerline/confidence context at about 5 Hz. Neither a Messages request, face identity, VLM label, nor Gemini prose can open tread authority.

\section{Gemini integration}

\Experimental\quad The entire Gemini integration defaults off and requires explicit enablement plus credentials. Once enabled, the current configuration defaults camera-frame and microphone-audio streaming on unless each stream is separately disabled. The robot-action tool remains separately off by default. Its schema declares \texttt{look\_at}, \texttt{play\_gesture}, \texttt{request\_pick}, \texttt{navigate\_relative}, and \texttt{stop\_motion}; the model never receives arbitrary raw serial output. Only \texttt{stop\_motion} has an immediate local executor, and even that returns a partial result because arm hold is unverified. The other actions have no deterministic executor: the controller console records approval or rejection but does not actuate hardware.

\Planned\quad General picking is intentionally unavailable. Missing prerequisites include calibrated camera-to-arm transforms, complete neck mapping, joint feedback, inverse kinematics, collision checking, grasp planning, and a verified executor.

\section{Local MLX, comedy, and battle training}

\ROBSourceNow{Cerebro/Cerebro/ROBStageShowCoordinator.swift}{Read the strict show protocol and fixture tests first, then compare \texttt{ROBMLXImprovisationProvider.swift}, \texttt{ROBSaberChoreography.swift}, and the JSON catalog under \texttt{StageShows}.}

The stage-show coordinator is a cancellable state machine rather than a sequence of arbitrary shell or motor commands. Show JSON may request speech, waits, checkpoints, allow-listed gestures, and adaptive dialogue; it may not contain raw joints, hosts, ports, or shell commands. Dry Run has no side effects. Offline uses authored lines. Local mode may use a schema-validated MLX or loopback llama.cpp line. Adaptive mode may give a trusted local plan to Gemini, but timeout and failure fall back through validated local text to mandatory authored text. Late model completions are correlated and discarded.

The comedy show demonstrates why fallback belongs to creative design: roughly ten minutes of timing cannot depend on one network or model response. Progressive saber training maps named profiles to bounded Cartesian transforms and duration. It must remain theatrical, slow, guarded choreography with soft props, a participant boundary, dry-run visualization, independent stop, transform, inverse-kinematics, and collision validation, and no autonomous contact. Vision may score timing or offer spoken coaching; pixels do not directly command the AMBER arms.

\section{Robot-action implementation status}

\begin{tabularx}{\textwidth}{>{\ttfamily\scriptsize}p{1.35in} p{1.35in} X}
\toprule
ACTION & CURRENT STATUS & CURRENT BEHAVIOR\\
\midrule
stop\_motion & Partial local executor & Preempts local coordinators, writes one neutral/braked base frame, and drops the base heartbeat. Base stop is software-observed; Amber arm hold remains unverified.\\
look\_at & Schema only & No deterministic local executor. Outside autonomy it may be offered to the manual approval console; approval does not move hardware.\\
play\_gesture & Schema only & No deterministic local executor. Approval does not play a physical gesture.\\
navigate\_relative & Schema only & No deterministic local executor. It is distinct from the social-roam coordinator.\\
request\_pick & Planned / unavailable & Rejected until transforms, inverse kinematics, collision checking, joint feedback, grasp planning, and a verified executor exist.\\
\bottomrule
\end{tabularx}

\begin{safetybox}
Language-model confidence is not evidence of physical safety. Treat model output as an untrusted proposal. Validate types, values, state, authority, freshness, workspace, and collision policy in deterministic local code; require human approval where risk warrants it; and make rejection an ordinary result.
\end{safetybox}

\ROBPhotoTwo{2025-detached-articulated-arm.jpg}{The detached arm makes reach, joint order, cabling, and manipulation limits concrete.}{2022-oakd-depth-camera.jpg}{The depth camera is one input to a manipulation experiment; a successful image alone is not a grasp-validation test.}

\ROBPlaceholder{Provide arm/neck calibration files, verified keyframes, joint feedback and limits, safe animation cancel behavior, speech privacy settings, autonomy speed/radius/obstacle requirements, Gemini model/release configuration, consent signage, action approval policy, and validation for every enabled tool.}

\part{Integration, operation, and maintenance}

\ROBChapterPhoto{Chapter 13}{Verification before motion}{2022-tracked-base-front.jpg}

\chapter{Test strategy and safety case}

\section{Requirements traceability}

Every test should trace to a requirement and hazard. A simple matrix includes requirement ID, hazard ID, source revision, test method, equipment, pass criteria, result, evidence path, reviewer, and date. ``It worked once'' is not a repeatable acceptance criterion.

\section{Progressive test ladder}

\begin{tabularx}{\textwidth}{>{\robcondensed\bfseries\color{ROBAccent}}p{0.6in} X X}
\toprule
LEVEL & SCOPE & REQUIRED EVIDENCE\\
\midrule
0 & Design review and hazard analysis. & Reviewed drawings, calculations, assumptions, and unresolved risks.\\
1 & Pure software fixtures and simulation. & Protocol, range, state, deadline, replay, and cleanup tests.\\
2 & Logic outputs to indicators or dummy loads. & Pin states, ordering, safe initialization, timeout waveform.\\
3 & One controller with a small surrogate or disconnected actuator. & Polarity, limits, current, fault and stop response.\\
4 & Full hardware on rated stands with guards. & Direction, speed cap, coast/brake, current, temperature, E-stop timing.\\
5 & Slow unloaded floor test in an exclusion zone. & Tracking, latency, stopping distance, spotter log, post-test inspection.\\
6 & Incremental loads and declared surfaces. & Repeatable operating envelope and thermal/current margins.\\
7 & Full show rehearsal. & Setup checklist, barrier, staffing, failure drills, privacy notice, teardown plan.\\
\bottomrule
\end{tabularx}

\section{Failure injection}

Test the robot's response when:

\begin{itemize}
  \item joystick input stops or becomes non-finite;
  \item Watch-to-phone relay disappears;
  \item controller packets duplicate, reorder, age, or exceed bounds;
  \item control network disappears while video continues;
  \item video decoder stalls or requests a keyframe;
  \item USB serial is unplugged or filled with malformed bytes;
  \item Arduino boots with IMU missing;
  \item an IR sensor sticks low or produces grass reflections;
  \item lidar data becomes stale or claims an impossible timestamp;
  \item DepthAI helper exits or the camera disconnects;
  \item an actuator reaches a mechanical stop;
  \item one motor direction is reversed;
  \item a credential is revoked during active control;
  \item phone and Vision clients request control simultaneously or the prior owner continues sending after takeover;
  \item either arm grip releases, arm telemetry becomes stale, or one per-arm authority expires;
  \item one of three camera roles reports the wrong MXID/socket or disconnects while the other feeds continue;
  \item a face comparison uses the wrong encoder, a replay image, a lookalike, corrupt ciphertext, or a missing gallery key;
  \item Messages supplies a group, non-allowlisted sender, old/repeated item, oversized image, cross-account memory query, or late administrator confirmation;
  \item recording reaches its free-space floor, loses one sensor, restarts a camera, or recovers an interrupted session;
  \item autonomy loses its sensor or leaves its radius;
  \item an AI tool proposes an unsupported or stale action;
  \item physical E-stop is pressed at each declared operating speed and pose.
\end{itemize}

\section{Measure stop behavior}

For each layer, measure detection time, command time, electrical response, mechanical response, and final state. Report surface, slope, load, battery state, speed, controller mode, firmware/software revision, sample rate, and uncertainty. ``600 ms watchdog'' is not the same as ``stopped within 600 ms.''

\begin{safetybox}
Use rated stands, restraints, remote observation, current limiting, a spotter, and an independent stop before intentionally injecting failures. Some failures should be tested only in simulation or on surrogate hardware.
\end{safetybox}

\ROBPlaceholder{Insert the actual hazard analysis, operating envelope, requirements matrix, unit and fixture reports, current/thermal data, watchdog timing, E-stop test, stopping distances on hard floor and grass, slope tests, flipper/actuator load tests, arm collision tests, and sign-off authority.}

\ROBChapterPhoto{Chapter 14}{Maker Faire operations}{2025-rob-side-full-body.jpg}

\chapter{Present ROB safely in public}

\section{Roles}

Assign at least these responsibilities:

\begin{itemize}
  \item \textbf{Lead operator}: owns arm/disarm and the primary controller.
  \item \textbf{Safety spotter}: watches the robot and crowd, keeps a hand near the physical E-stop, and may stop without asking.
  \item \textbf{Educator}: explains ROB without distracting the operator.
  \item \textbf{Technical support}: monitors power, temperature, logs, and recovery.
  \item \textbf{Event liaison}: coordinates space, charging, barriers, accessibility, and emergency requirements.
\end{itemize}

One person may fill multiple roles only if workload and risk allow it. Never make the operator answer a long technical question while ROB moves near visitors.

\section{Pre-show checklist}

\begin{itemize}[label={\protect\raisebox{0.08em}{\protect\fbox{\protect\rule{0pt}{0.55em}\protect\rule{0.55em}{0pt}}}}]
  \item current approved software/firmware versions recorded;
  \item batteries inspected, charged under the approved procedure, and restrained;
  \item fuses, disconnect, and E-stop visually inspected and function-tested;
  \item treads, chain, sprockets, guards, shafts, collars, mounts, fasteners, and torque marks inspected;
  \item arm, neck, flipper, and actuator limits checked in a protected area;
  \item controllers uniquely paired; unused credentials revoked;
  \item control ownership, per-arm authority, and all enabled video feeds tested under venue network conditions;
  \item speed cap and operating mode verified on every controller;
  \item barrier, route, slope, floor transitions, cables, exits, and charging location inspected;
  \item camera, microphone, face recognition, Messages, transcript memory, recording, location, and cloud-AI policy displayed;
  \item fire response, first aid, lift/transport, and fault-recovery plans briefed;
  \item spare tools and parts controlled so visitors cannot reach them;
  \item dry rehearsal completed with the full team.
\end{itemize}

\section{Show-state model}

Use explicit visible states: Transport Safe, Display Only, Demo Armed, Moving, Stopped, Faulted, Charging, and Maintenance. Each transition names who authorizes it and what checklist applies. Reconnecting a controller or restarting software never silently arms motion.

\section{Audience and privacy}

Keep an accessible viewing route. Do not require visitors to step over cables. Mark pinch and tread boundaries clearly. Avoid publishing or enrolling faces without consent. When camera, microphone, face recognition, Messages, transcript memory, recording, or cloud processing is active, use plain-language signage and a visible indicator; provide a quick disable/delete path and a non-recorded, non-enrolled way to participate. A recognized identity is personalization only and never event admission, controller ownership, or action authority.

\ROBPlaceholder{Insert venue-specific barrier dimensions, maximum public-demo speed, operator and spotter names, E-stop placement, battery/charging policy, transport vehicle and lift plan, insurance or venue requirements, photo/video release plan, privacy signage, scripted demonstrations, and abort criteria.}

\ROBChapterPhoto{Chapter 15}{Maintenance and change control}{2025-rear-electronics-bay.jpg}

\chapter{Keep the robot understandable}

\section{Before and after every operating day}

Inspect for loose fasteners, moved torque marks, chain and tread damage, shaft-collar movement, bracket cracks or bending, wire abrasion, connector heating, battery swelling or damage, fluid or debris, blocked ventilation, actuator looseness, unusual sound, camera security, E-stop function, and log errors.

After operation, record runtime, surfaces, mode, maximum observed current and temperature if available, incidents, unexpected stops, repairs, and deferred defects. Quarantine the robot from powered use when a red condition remains unresolved.

\section{Scheduled maintenance record}

For every interval, name the component, task, acceptable condition, tool, replacement part, torque or measurement, evidence, and sign-off. Intervals may be hours, cycles, calendar time, or before each public show.

\section{Configuration control}

A change to code, pin mapping, motor polarity, brake logic, network framing, controller speed cap, arm limit, battery arrangement, or bracket can invalidate earlier tests. Give hardware, firmware, software, calibration, wiring, and operating procedure separate revision identifiers, then record the tested combination.

\section{Troubleshooting by layer}

\begin{tabularx}{\textwidth}{>{\robcondensed\bfseries\color{ROBAccent}}p{1.35in} X}
\toprule
SYMPTOM & SAFE DIAGNOSTIC PATH\\
\midrule
No controller connection & Keep motion disarmed; verify unique pairing, robot ID, Bonjour visibility, certificate pin, role, revocation, and session proof.\\
Control works, no video & Keep control independent; inspect the separate video service, camera permission/state, capability handshake, session binding, and decoder recovery.\\
Arduino not online & Disconnect actuator power as required; verify USB device, 250,000 baud, startup log, safe GPIO state, and dependency/IMU behavior.\\
One tread reverses & Stop and lift/guard the base; compare requested sign, adapter output, Arduino pin, driver polarity, and physical mount. Do not fix by guessing in UI code.\\
Unexpected IR warnings & Disable motion; log all raw readings with surface, angle, light, and distance; inspect shrouds and wiring; keep IR outside certified safety claims.\\
Autonomy will not move & Treat as safe failure; verify explicit authorization, fresh lidar, activation radius, master control, and local planner state.\\
Arm action rejected & Treat rejection as correct until capability, calibration, approval, and executor prerequisites are satisfied.\\
\bottomrule
\end{tabularx}

\ROBPlaceholder{Add maintenance intervals, lubrication policy, tread and chain wear limits, fastener torques, battery retirement criteria, cable replacement rules, calibration intervals, spare-parts inventory, fault codes, backup/restore procedure, and configuration-release form.}

\part{Advanced engineering compendium}
\setlength{\emergencystretch}{4em}
\sloppy

\begin{advancedbox}[title={WHY THE COMPLETE MANUAL INCLUDES THE FULL ADVANCED VOLUMES}]
The hardware chapters above establish ROB's physical plant, evidence status, and operating boundaries. The following four manuscripts preserve the complete software and arm tutorials beside that record: AI-assisted engineering practice, AMBER/Ubuntu/CAN/LCM, the Vision Pro controller, and Cerebro's mixed-language perception/model/video architecture. They are included as real instructional content, not page-count filler. When a repeated concept appears, read the subsystem volume for code-level detail and the earlier field-manual chapter for the whole-robot safety boundary.
\end{advancedbox}

\part{AI-assisted robotics engineering}
\markdownInput{volume-5-ai-robotics-with-codex.md}

\part{AMBER B1, Ubuntu, CAN, and LCM}
\markdownInput{volume-6-amber-dual-arm-robotics.md}

\part{Vision Pro control and media client}
\markdownInput{volume-7-engineering-robcontrollervision.md}

\part{Cerebro perception, models, and H.264}
\markdownInput{volume-8-engineering-cerebro.md}

\appendix

\input{adult-learning-and-commissioning.tex}

\chapter{Builder input ledger}

This appendix is the iteration queue. Replace each line with a dated, reviewed answer and evidence reference.

\begin{enumerate}
  \item Complete the first-person project origin: name meaning, motivation, contributors, shops, and the relationship between the now-documented 2017-2025 Cerebro source history and ROB's physical revisions.
  \item Final overall dimensions, ready-to-run mass, center of mass, load cases, operating surfaces, slopes, speed, and environmental limits.
  \item As-built CAD/templates, plate materials and thicknesses, fasteners, shafts, bearings, sprockets, chains, tension procedure, guards, and coatings.
  \item Actual milling, drilling, bending, welding, deburring, finishing, workholding, tooling, feeds/speeds, inspection, PPE, and vendor sequence.
  \item Current tread and flipper motors, windings, gearbox ratios, controller circuits, brake polarity, current limits, and performance tests.
  \item Flipper location and geometry, third-wheel status, actuator roles, models, strokes, loads, end stops, feedback, and failure behavior.
  \item Battery packs, topology, BMS, charger, fusing, main disconnect, E-stop, converters, inverter, grounding, wire gauges, connectors, and power budget.
  \item Arduino-compatible board model, core and libraries, pin-to-connector harness, command ranges, deadman timing, IMU calibration, IR positions/status, and speed-feedback status.
  \item Current Mac mini/Raspberry Pi inventory, OS versions, ports, network devices, router role, serial-device ownership, and dependency versions.
  \item Arm and neck models, geometry, calibration, zero/home, soft/hard limits, payload, current/torque limits, collision envelope, and safe transport.
  \item Camera and lidar inventory, mounts, coordinate transforms, calibration, timestamp synchronization, accuracy, lighting, and present host path.
  \item Controller compatibility, pairing/revocation, speed caps, latency, watchdogs, location-data policy, and Vision Pro validation.
  \item Autonomy operating envelope, sensor requirements, aborts, AI model/tool configuration, approval policy, and camera/microphone/cloud consent.
  \item Hazard analysis, protective measures, E-stop proof, test reports, Maker Faire perimeter and staffing, fire, lifting, and charging plans, plus insurance and venue requirements.
  \item Final photo captions, permissions/releases, vendor-publication permissions, credits, and redacted screenshots.
\end{enumerate}

\chapter{Bill of materials worksheet}

\begin{tabularx}{\textwidth}{p{0.35in} p{1.15in} X p{0.55in} p{0.75in} p{0.8in}}
\toprule
ID & SUBSYSTEM & MANUFACTURER / PART / REVISION & QTY & SOURCE & STATUS\\
\midrule
\rule{0pt}{0.34in} & & & & &\\
\rule{0pt}{0.34in} & & & & &\\
\rule{0pt}{0.34in} & & & & &\\
\rule{0pt}{0.34in} & & & & &\\
\rule{0pt}{0.34in} & & & & &\\
\rule{0pt}{0.34in} & & & & &\\
\rule{0pt}{0.34in} & & & & &\\
\rule{0pt}{0.34in} & & & & &\\
\bottomrule
\end{tabularx}

For each line add voltage, current, mechanical interface, firmware, calibration, supplier status, substitute, unit cost, lead time, safety documentation, and evidence photograph in the editable project record.

\chapter{Wiring and connector worksheet}

\begin{tabularx}{\textwidth}{p{0.55in} p{1in} p{1in} p{0.65in} p{0.58in} p{0.7in} X}
\toprule
NET & FROM & TO & VOLTS & MAX A & WIRE & PROTECTION / CONNECTOR\\
\midrule
\rule{0pt}{0.34in} & & & & & &\\
\rule{0pt}{0.34in} & & & & & &\\
\rule{0pt}{0.34in} & & & & & &\\
\rule{0pt}{0.34in} & & & & & &\\
\rule{0pt}{0.34in} & & & & & &\\
\rule{0pt}{0.34in} & & & & & &\\
\bottomrule
\end{tabularx}

Also record return path, chassis bond, shield termination, cable length, routing class, strain relief, flex cycles, color/label, fuse/breaker location, test point, polarity check, and continuity/insulation result.

\chapter{Serial protocol reference}

\section*{Historical base frame}

\begin{tabularx}{\textwidth}{>{\robcondensed\bfseries}p{0.65in} p{1.55in} p{1.1in} X}
\toprule
FIELD & MEANING & EXAMPLE & CURRENT LIMIT / NOTE\\
\midrule
Start & Frame marker & \texttt{\textasciitilde} & One byte\\
1 & Left brake & \texttt{+0000} & 0 described as release; nonzero engage\\
2 & Left signed speed & \texttt{+0100} & Expected magnitude within 0-255; not clamped\\
3 & Right brake & \texttt{+0000} & Same mapping\\
4 & Right signed speed & \texttt{+0100} & Mirrored physical direction polarity\\
5 & Flipper brake & \texttt{+0000} & Same mapping\\
6 & Flipper signed speed & \texttt{+0000} & Expected magnitude within 0-255; not clamped\\
7 & Actuator signed speed & \texttt{+0000} & Nominal -3200 to +3200; +3201 and -3201 clear safe start, then command +3200 and -3200 respectively\\
\bottomrule
\end{tabularx}

Fields are five ASCII characters and separated by six commas. Total historical frame length is 42 bytes. The current decoder defects listed in Chapter 7 must be fixed before treating the format as safe. A replacement should add explicit version, message type, bounded length, sequence, integrity check, and typed state transitions.

\chapter{Repository source map}

\begin{tabularx}{\textwidth}{>{\ttfamily\scriptsize}p{2.65in} X}
\toprule
RELATIVE PATH & PUBLICATION USE\\
\midrule
Presentation/ROB\_v3.pdf & 95-page historical build and design deck; slides dated 2021 with 2022 modifications.\\
Cerebro v1-v5 workshop archive & Historical Mac-software evidence: the pre-Git serial-command seed, the 2018 Git root, perception/control branches through 2022, Apple-silicon and LLM work through July 2025, and the fresh v5 repository boundary. Fingerprints and cautions are recorded in \texttt{ROB-Books/CEREBRO\_ARCHIVE\_HISTORY.md}.\\
ROBArduino/ROBOT\_CEREBELLULAR\_BASE\_APP/...ino & Present Base-role reference: pin map, parser, treads, flipper, actuator, IR, IMU, heartbeat.\\
ROBArduino/ROBOT\_CEREBELLULAR\_TORSO\_APP/...ino & Retired Torso-role history: Maestro head/arm targets, IMU, and disabled GPS/timeout behavior.\\
ROBArduino/ROBOT\_CEREBELLULAR\_HEAD\_APP/...ino & Retired Head-role history: IMU/PIR experiment, disabled GPS path, incomplete copied parser.\\
AmberHomeFolder/amber/L-10 and R-11 & Captured Ubuntu runtime evidence for the two AMBER cores; launch configuration binds left/right CAN interfaces, LCM prefixes, UDP ports, solvers, and URDFs. Treat the broader home snapshot as private forensic material, not a deployable image.\\
AmberHomeFolder/amber/sin\_wave/rawLcm & Source definitions for joint targets, arm feedback, Cartesian tasks, plans, solver responses, and gripper control.\\
Amber URDF/amber\_b1.urdf & Independent seven-joint single-arm geometry and model limits; requires measured ROB mounting and tool transforms.\\
Cerebro/Amber-PythonAPI/Amber V2 API & Packed UDP reference client for status, motion, mode, calibration, and gripper commands; requires protocol hardening before production authority.\\
ROB hardware spec datasheets/Hengdrive*.pdf & Controlled builder-owned B5685G outline/performance records; selected interface facts summarized, full drawings excluded pending publication permission.\\
ROB hardware spec datasheets/B-COMMAND*.pdf & Controlled custom rotary-interface drawings for the installed USB3/24 V torso unit and the intended USB3/Ethernet/12 V head unit; selected facts summarized, drawings excluded.\\
ORobotics/media/gallery-originals & Private high-resolution construction and event photographs; strip metadata and review releases.\\
ORobotics/data/gallery\_metadata.json & Visual descriptions useful for search and draft captions, not authoritative part identification.\\
Cerebro/Cerebro/ROBSerialBox.m & Physical serial adapter, base heartbeat, servo/arm helpers.\\
Cerebro/Cerebro/ROBMainViewController.mm & Operator coordination, controller/action/autonomy integration.\\
Cerebro/Cerebro/CameraManager.swift & Camera, RGB-D helper, and bounded frame delivery.\\
Cerebro/Cerebro/ROBAutonomyCoordinator.swift & Session-bounded local autonomy.\\
Cerebro/Cerebro/ROBAI.swift & Optional multimodal Gemini path.\\
Cerebro/Cerebro/GeminiRoboticsProtocol.swift & Versioned high-level action protocol.\\
\bottomrule
\end{tabularx}

\section*{Advanced software source map}

\begin{tabularx}{\textwidth}{>{\ttfamily\scriptsize}p{2.65in} X}
\toprule
RELATIVE PATH & PUBLICATION USE\\
\midrule
Cerebro/Cerebro/ROBSceneSnapshot.swift & Typed people, object, free-space, arm/camera-pose, quality, and confidence boundary for models.\\
Cerebro/Cerebro/ROBMLXRuntime.swift & Actor-owned local LLM, VLM sampling, embeddings, bounded semantic memory, and diagnostics.\\
Cerebro/Cerebro/ROBInsta360*.swift & Headless panoramic camera control, demand-driven decode, diagnostics, and six-sector perception.\\
Cerebro/Cerebro/ROBRecordingCoordinator.swift & Recoverable synchronized sensor, pose, authority, label, and movie sessions.\\
Cerebro/Cerebro/ROBFace*.swift & Consent enrollment, encrypted model-tagged gallery, selectable AdaFace embeddings, recognition gates, and identity UI.\\
Cerebro/Scripts/install\_adaface\_models.py & Checkpoint hashing, FP16 Core ML conversion, PyTorch agreement validation, compilation, and local manifest.\\
Cerebro/Cerebro/ROBMessages*.swift & Allowlisted one-to-one bridge, responder, vision reply policy, encrypted memory/browser, current information, and administrator-command UI.\\
Cerebro/Cerebro/ROBVideoProtocol.swift & RVID/RBVD video framing, negotiation, limits, codec configuration, and access units.\\
Cerebro/Cerebro/ROBCameraH264Encoder.swift & VideoToolbox NV12 scaling, real-time H.264 encoding, SPS/PPS, AVCC, and recovery output.\\
Cerebro/Cerebro/ROBVideoServer.swift & Authenticated session-bound video subscriptions, bounded send state, feedback, and liveness.\\
Cerebro/Cerebro/ROBStageShow*.swift & Strict show schema, cancellable cue state machine, local/cloud fallback, and diagnostics.\\
Cerebro/Cerebro/ROBSaberChoreography.swift & Allow-listed progressive training transforms and duration profiles.\\
Cerebro/Cerebro/KeyframeAnimationManager.swift & Persisted animation data.\\
Cerebro/docs & Current transport, video, depth, autonomy, recording/training, Messages, face identity, and Gemini design notes.\\
ROBController/Consciousness & iPhone/iPad map/command UI, control ownership, destination autonomy, pairing, Watch relay, push-to-talk, and action console.\\
ROBController/Consciousness-Watch Watch App & Watch user interface and relay intent.\\
ROBControllerVision & Vision Pro controller, explicit ownership, independent arm authorities, three-feed/immersive presentation, simulator, secure adapter, and tests.\\
ROBControllerVision/.../ArmControlProtocol.swift & \texttt{rob-arm-control/2} feedback, authority, target, hold, lease, and strict wire boundaries.\\
ROBControllerVision/.../ROBVideoPipeline & H.264 validation, SPS/PPS format construction, AVCC sample buffers, renderer recovery, and bounded synthetic fixtures.\\
M2M1-RPLIDAR-iOS-MacOS-Catalyst-/RPLidar & Slamware acquisition, compact RLS1 scans, authenticated local/QUIC publication, map persistence, and OSM alignment.\\
\bottomrule
\end{tabularx}

\chapter{Primary technical references}

Repository paths document ROB's implementation. The following manufacturer and standards sources document the component or protocol boundary. Capture a dated PDF/hash for any reference used to release hardware; web pages can change.

\begin{tabularx}{\textwidth}{>{\robcondensed\bfseries}p{1.3in} X}
\toprule
TOPIC & PRIMARY REFERENCE\\
\midrule
LT2 platform & SuperDroid Robots, LT2 support and current configurable platform: \url{https://www.superdroidrobots.com/wiki-a/index_php/LT2_and_LT2-F_Robot_Support} and \url{https://www.superdroidrobots.com/store/product=1513}. Current catalog options do not identify ROB's purchased revision.\\
Arduino Mega & Arduino Mega 2560 Rev3 hardware page, pinout, datasheet, and schematic: \url{https://docs.arduino.cc/hardware/mega-2560}.\\
Arduino GIGA & Arduino GIGA R1 WiFi hardware page, pinout, datasheet, and schematic: \url{https://docs.arduino.cc/hardware/giga-r1-wifi}.\\
Motor serial & Pololu Simple Motor Controller User's Guide, compact protocol and safe-start commands: \url{https://www.pololu.com/docs/0J44/all}.\\
Stepper control & Pololu Tic Stepper Motor Controller User's Guide: \url{https://www.pololu.com/docs/0J71/all}. Identify ROB's exact Tic before applying ratings.\\
Neck servo & Hitec HSB-9380TH product/specification page: \url{https://hitecrcd.com/hsb-9380th-ultra-torque-brushless-titanium-gear-servo/}.\\
OAK cameras & Luxonis OAK-D Pro W hardware page and device-information example: \url{https://docs.luxonis.com/hardware/products/OAK-D\%20Pro\%20W} and \url{https://docs.luxonis.com/software-v3/depthai/examples/misc/device_information/}.\\
RPLidar & SLAMTEC product-specific datasheet and SDK selected after the installed model is identified; do not merge A1 and S2E interface claims.\\
LCM & Official LCM introduction and Python tutorial: \url{https://lcm-proj.github.io/lcm/content/tutorial.html} and \url{https://lcm-proj.github.io/lcm/content/tutorial-python.html}.\\
SocketCAN & Linux kernel SocketCAN documentation: \url{https://docs.kernel.org/networking/can.html}.\\
CAN physical layer & Texas Instruments, \textit{Controller Area Network Physical Layer Requirements}, SLLA270; termination and grounding must also follow installed transceiver/vendor guidance.\\
H.264 transport & IETF RFC 6184, \textit{RTP Payload Format for H.264 Video}: \url{https://www.rfc-editor.org/rfc/rfc6184}. ROB uses AVCC over custom reliable QUIC, not RTP; RFC 6184 is used to teach NAL concepts and future RTP packetization distinctions.\\
Apple H.264 decode & Apple Core Media, \texttt{CMVideoFormatDescriptionCreateFromH264ParameterSets}: \url{https://developer.apple.com/documentation/coremedia/cmvideoformatdescriptioncreatefromh264parametersets}.\\
\bottomrule
\end{tabularx}

The B-COMMAND and HengDrive drawings in the builder's hardware folder remain controlled vendor records. Their model numbers and selected interface facts are recorded in the relevant chapters; obtain written permission before reproducing the drawings or a substantial table of their contents.

\chapter{Photo and artwork credits}

All construction photographs in this edition originate from the private Orbitus Robotics gallery archive and are credited to Rodolfo Aramayo / Orbitus Robotics unless the builder supplies a different credit. The frontispiece and software-control illustrations are original generated artwork derived from ROB reference photographs and created for this book set. They contain no official franchise logo, character, or copied page design and are labeled as illustrations rather than engineering evidence.

The book's visual system uses licensed system typefaces and an original weathered retro-space workshop palette. The project is independent and is not affiliated with or endorsed by any film studio, franchise owner, component vendor, or platform company.

Before public printing, complete image releases, confirm third-party product-image and datasheet permissions, remove private metadata and secrets, and obtain a final legal/trademark review appropriate to the distribution plan.

\vfill
\begin{missionbox}[title={NEXT ITERATION}]
Work through the Builder input ledger with the machine physically available. Replace placeholders with measured values, drawings, test reports, verified captions, and contributor stories. Then tag the source snapshot, rebuild all ten PDFs, and perform a page-by-page technical and print proof before the Maker Faire print run.
\end{missionbox}

\ROBSource{This field manual was synthesized from the local ROB presentation, builder interview of 2026-08-21, private photo originals and metadata, builder-owned hardware records, official manufacturer/standards references, Arduino firmware, archived Cerebro v1-v5 source, and the current Cerebro, ROBController, local ROBControllerVision, RPLidar, Amber-HomeFolder, ROBTrainingGames, and ORobotics repositories inspected through 2026-08-23. The current pass restores Cerebro's 2017-2025 lineage and documents the 2025 v5 Git reset as a migration boundary.}

\end{document}
